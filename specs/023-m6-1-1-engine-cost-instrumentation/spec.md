# Feature Specification: M6.1.1 — Engine-Cost Instrumentation Diagnosis & Symmetrisation

**Feature Branch**: `023-m6-1-1-engine-cost-instrumentation`
**Created**: 2026-05-16
**Status**: Draft
**Input**: User description: "M6.1.1 as described in PLAN.md"

## Background *(non-normative summary)*

M6.1's full-sweep verdict report ([docs/benchmarks/m6_1-real-prompt-embeds.md](../../docs/benchmarks/m6_1-real-prompt-embeds.md)) raised `engine_cost_drift_warning` on **all three chat_stream cells** (`c=1`, `c=4`, `c=8` at `hidden_size=4096`). The per-cohort spread on `engine_ttft_ms` was consistent across all three cells at roughly 14–17%: `rest_https_edge ≈ 43.5 ms / default_grpc ≈ 47.5 ms / tuned_grpc_multiplexed ≈ 41.5 ms`. The same engine code path served every RPC in those cells; only the transport differed. So either:

- **Hypothesis A — Measurement-window asymmetry.** REST captures `engine_start = perf_counter()` inside the FastAPI shim (a→ first SSE chunk yielded), while gRPC reads `engine_ttft_ms` from server-side trailing metadata anchored on a slightly different point. The two clocks straddle different windows; the engine is doing the same work but the reported number is bracketing different segments. This would be a measurement artifact: harmless to the engine but contaminating any per-cohort engine-cost decomposition published from M6.1 onward.
- **Hypothesis B — Channel-dependent batching.** vLLM's continuous batching sees REST and gRPC arrival patterns differently (HTTPS edge buffers requests with different jitter than a raw TCP gRPC channel), and the engine schedules them into different batches with measurably different first-token latencies. This would be a real effect, not an artifact — operationally significant: it means protocol choice changes the engine's behavior, not just the wire-cost differential.

M6.1.1 distinguishes the two with a multi-point timing diagnosis sweep, then either fixes the instrumentation (Hypothesis A) or publishes the finding as a methodology supersedence (Hypothesis B). The decision is data-driven, not pre-committed.

**Why M6.1.1 must close before M6.2 (token-budget axis) opens.** M6.2 introduces a `max_tokens` measurement axis. Bundling instrumentation work with the axis change would make the M6.2 diff against M6.1 ambiguous (did the per-cohort engine_ttft shift because of longer generation, or because the measurement window was fixed?) — exactly the methodological confound Spec Kit Phase Discipline (one-variable-per-milestone) is designed to prevent. Two further concrete reasons: (1) M6.2's `max_tokens=10` / `50` null anchors must reproduce M6.1's measurements within published CIs to validate sweep integrity; if the instrumentation gap is constant, every anchor on every cap will fire `engine_cost_drift_warning` regardless of whether the rest of the sweep is sound, and the anchor mechanism stops working. (2) The same gap will recur in M7 (corpus diversity) and M8 (multi-model); every per-cohort engine-cost decomposition published from now on will carry a 14–17% asterisk until it is resolved. Cheap to fix once now; expensive to retrofit later.

## User Scenarios & Testing *(mandatory)*

### User Story 1 — Diagnose the chat_stream engine_ttft spread via multi-point timing (Priority: P1)

The operator runs a focused mini-sweep that emits four timestamps per chat_stream RPC from both REST and gRPC paths — at handler entry, just before `engine.generate(...)`, at the first streamed chunk, and at terminal emit — so the published M6.1 `engine_cost_drift_warning` can be attributed to either measurement-window asymmetry (instrumentation artifact) or channel-dependent batching (real engine effect). Without this attribution, M6.2 cannot use the M6.1 verdicts as a null anchor and every downstream milestone's per-cohort engine-cost decomposition carries an unresolved 14–17% asterisk.

**Why this priority**: This is the entire milestone's reason for existing. Without Phase 1's diagnostic data, Phase 2 cannot be chosen, M6.1's published table cannot be defended past the next sweep, and M6.2 cannot start. Every other user story in this spec is downstream of Phase 1's classification output.

**Independent Test**: Operator runs `python -m vllm_grpc_bench --m6_1_1-diagnose --m6_1_1-modal-region=eu-west-1` against the matrix M6.1 used (6 cells × 3 cohorts × n=50). The harness publishes `docs/benchmarks/m6_1_1-engine-cost-instrumentation.{md,json}` containing: (a) a per-cohort multi-point timing table for each of the 6 cells (the three embed cells included as controls; per FR-031 / Out of Scope, drift attribution is computed only for chat_stream), (b) per-segment delta analysis (a→b, b→c, c→d) per cohort per chat_stream cell, and (c) one of four classifications per chat_stream cell: `instrumentation_artifact` / `channel_dependent_batching` / `inconclusive` / `drift_not_reproduced`. The operator can read the classification and the segment carrying the spread without running anything further.

**Acceptance Scenarios**:

1. **Given** M6.1's published JSON exists at the default baseline path, **When** the operator runs `--m6_1_1-diagnose` against an M6.1.1-prepared Modal deployment, **Then** the harness publishes a markdown + JSON report containing 4-checkpoint timings per cohort per cell, per-segment deltas, and one of {`instrumentation_artifact`, `channel_dependent_batching`, `inconclusive`, `drift_not_reproduced`} for each of the 3 chat_stream cells.
2. **Given** M6.1's published JSON is missing or unreadable, **When** the operator runs `--m6_1_1-diagnose`, **Then** the harness exits with code `1` and prints a stderr message naming the missing baseline file and the `--m6_1_1-m6-1-baseline` flag for overriding it.
3. **Given** the operator's local `torch` version does not match the pin enforced in M6.1, **When** the operator runs `--m6_1_1-diagnose`, **Then** the harness exits with code `2` BEFORE attempting any Modal deployment, identical to M6.1's FR-006 gate, and prints the expected vs actual version.

---

### User Story 2 — Symmetrise the engine_ttft measurement window OR publish channel-dependent batching as a finding (Priority: P2)

Based on Phase 1's classification, the operator either (a) applies a code fix that moves the REST or gRPC bracketing so both paths capture the same engine work, then runs an n=100 verification sweep against the M6.1 matrix to confirm the drift warning clears; or (b) treats the spread as a real channel-dependent batching effect, updates `contracts/instrumentation.md` with an operator-facing interpretation, and annotates M6.1's published JSON with a methodology supersedence pointer to M6.1.1's diagnosis. Path choice is dictated by Phase 1's data, never pre-committed.

**Why this priority**: Phase 2 turns Phase 1's diagnosis into a closed methodology gap. Without Phase 2, M6.1's published table still carries the drift asterisk forever; M6.2 still cannot use M6.1 as a null anchor. P2 (not P1) because Phase 2 cannot run until Phase 1 finishes — strict ordering — but they are part of the same milestone.

**Independent Test**: If Phase 1 returned `instrumentation_artifact`, the operator applies the symmetrisation code change to the path Phase 1 identified, runs `python -m vllm_grpc_bench --m6_1_1 --m6_1_1-modal-region=eu-west-1`, and the harness publishes a Phase-2(a) verification sweep that shows `engine_cost_drift_warning=false` across all three chat_stream cells (each cohort's `engine_ttft_ms` mean within 5% of the unweighted cohort-average for that cell). If Phase 1 returned `channel_dependent_batching`, the operator runs a doc-only commit updating `contracts/instrumentation.md` and the M6.1.1 markdown report; the M6.1.1 JSON records the chosen path so M6.2 (which reads it as a hard precondition) can interpret M6.1's per-cohort engine_ttft correctly.

**Acceptance Scenarios**:

1. **Given** Phase 1 classified all three chat_stream cells as `instrumentation_artifact` and a code change has symmetrised the bracketed window, **When** the operator runs `--m6_1_1` (Phase 2(a) verification sweep, n=100), **Then** the harness publishes a verification report showing `engine_cost_drift_warning=false` for all three chat_stream cells AND each cohort's `engine_ttft_ms` mean within 5% of the unweighted cohort-average for that cell, AND M6.1's published JSON is annotated with a forward pointer to the M6.1.1 verification report.
2. **Given** Phase 1 classified the chat_stream drift as `channel_dependent_batching`, **When** the operator updates `contracts/instrumentation.md` with a section explaining the effect and the operator-facing interpretation of per-cohort `engine_ttft_ms` differences, **Then** the M6.1.1 markdown report records the doc-only Phase 2(b) outcome, M6.1's published JSON is annotated with a forward pointer naming the channel-dependent-batching finding, and no verification sweep is run.
3. **Given** Phase 1 returned `inconclusive` (the spread is split across multiple segments, not isolated to one), **When** the operator inspects the Phase 1 markdown report, **Then** the report explicitly names the segments involved and recommends a follow-up action (additional checkpoints, longer-running mini-sweep, or operator-discretion path selection), and the milestone does NOT auto-proceed to Phase 2.
4. **Given** Phase 1 returned `drift_not_reproduced` (the mini-sweep itself shows the per-cohort `engine_ttft_ms` spread within 5% of unweighted cohort-average across all three chat_stream cells), **When** the operator inspects the report, **Then** the report flags M6.1's published warning as a likely transient-infrastructure artifact and recommends one of {re-run Phase 1 once more for confirmation; accept and close M6.1.1 with a `drift_not_reproduced` annotation on M6.1's JSON}.

---

### User Story 3 — Keep M6.1's published verdict table valid via methodology supersedence rather than re-sweep (Priority: P3)

The operator preserves M6.1's published verdict table (the `supersedes_m6_under_enable_prompt_embeds` rows are unchanged, the cohort selection rules are unchanged, the cell shape is unchanged) and adds a methodology supersedence pointer from M6.1's JSON to M6.1.1's published report. This lets readers of M6.1's table reach the up-to-date interpretation of `engine_cost_drift_warning` without re-running M6.1.

**Why this priority**: P3 because the verdict table itself remains valid — the drift warning was diagnostic, never a verdict gate (FR-022 in M6.1 makes the flag non-blocking). The supersedence pointer is a small documentation step but it has to land for the methodology trail to be unbroken.

**Independent Test**: A reader who opens `docs/benchmarks/m6_1-real-prompt-embeds.md` and scrolls to the chat_stream section sees a one-line note: "Methodology supersedence: see `docs/benchmarks/m6_1_1-engine-cost-instrumentation.md` for the diagnosis and resolution of the `engine_cost_drift_warning` reported on these cells." The M6.1 JSON carries the same pointer in a top-level `methodology_supersedence` field. M6.2 (when it runs) discovers the pointer programmatically and incorporates the M6.1.1 finding into its null-anchor logic.

**Acceptance Scenarios**:

1. **Given** M6.1.1's report has been published (regardless of Phase 2 path chosen), **When** the operator commits the methodology supersedence annotation to M6.1's JSON and markdown, **Then** M6.1's published artifacts contain a forward pointer naming the M6.1.1 report, the M6.1 JSON's top-level schema gains a `methodology_supersedence` key, and a deterministic test verifies the pointer's target file exists.
2. **Given** M6.1 originally published `supersedes_m6_under_enable_prompt_embeds` verdicts, **When** M6.1.1 lands, **Then** those rows MUST NOT be re-computed; M6.1.1 is a differential report only, never a verdict re-run for the embed cells (chat_stream cells under Phase 2(a) verification are an exception, but those are scoped to the drift-cleared check, not to re-classifying M6.1's verdicts).

---

### Edge Cases

- **Phase 1 mini-sweep produces a 4-segment timing in which the spread is concentrated in `c→d` (post-engine, pre-emit) on one path only** — not anticipated by the (a) measurement-window-asymmetry / (b) channel-dependent-batching dichotomy. Handling: classifier returns `inconclusive`; report names the segment and recommends additional checkpoints (e.g., splitting the post-engine emit segment) in a follow-up M6.1.1 round before declaring Phase 2 path.
- **Phase 1 mini-sweep itself shows no per-cohort spread above 5% on chat_stream cells** (M6.1's reported drift was a transient infrastructure artifact rather than a persistent issue). Handling: classifier returns `drift_not_reproduced`; report flags the M6.1 reading as likely transient; operator chooses between confirming with a second Phase 1 run or accepting the `drift_not_reproduced` annotation.
- **M6.1's published JSON is moved, renamed, or unreadable when M6.1.1 starts.** Handling: hard precondition; harness exits with code `1` and instructs the operator to pass `--m6_1_1-m6-1-baseline=<path>` explicitly.
- **The four timing checkpoints themselves perturb engine_ttft enough to shift the (b→c) measurement vs M6.1's per-cohort baseline.** Handling: instrumentation is timestamp-recording only (`perf_counter_ns()` on the local process, no network I/O at the checkpoints, no logging); perturbation budgeted at <500 µs per checkpoint and asserted in the Phase 1 report's methodology note. If the budget is exceeded a stderr warning fires and the operator is asked to weight the Phase 1 numbers cautiously.
- **gRPC trailing metadata size grows from the 4 new keys enough to affect protocol overhead vs M6.1.** Handling: 4 keys × ≤20 bytes each ≈ 80 bytes of additional metadata, well below the ~1 KB metadata budget M6 already operates within. No mitigation expected; report's methodology section records the actual increase in bytes for audit.
- **The REST shim's SSE terminal event already carries some per-RPC instrumentation fields (M6's `engine_ttft_ms`, `engine_tpot_ms`); adding four more JSON fields could collide with existing keys.** Handling: the four new keys are namespaced under a `m6_1_1_timings` sub-object on the terminal SSE JSON event so the existing M6 field set remains exactly as it was; the namespace is recorded in `contracts/instrumentation.md`.
- **gRPC chat_stream trailing metadata already carries the M6 `engine_ttft_ms` and `engine_tpot_ms` keys; the four new keys could collide.** Handling: new keys are prefixed `m6_1_1_t_` (e.g., `m6_1_1_t_handler_entry`, `m6_1_1_t_pre_engine`); the prefix is recorded in `contracts/instrumentation.md`.
- **Phase 2(a) verification sweep also fires `chat_stream_control_drift_warning` (FR-029 from M6.1)** because the symmetrised instrumentation changes the measurement window enough that M6.1's published chat_stream CI no longer overlaps the new one. Handling: this is expected; the verification report explains that the control-drift flag in Phase 2(a) reflects the methodology change, not infrastructure drift, and is treated as informational (verdicts in Phase 2(a)'s scope are limited to the `engine_cost_drift_warning` cleared/persistent decision; no chat_stream verdicts are re-published).
- **The drift warning fires only on a subset of the three chat_stream cells when re-measured (e.g., c=8 but not c=1, c=4).** Handling: per-cell classification (FR-010) — each of the three chat_stream cells gets its own label; Phase 2 path is chosen per-cell if labels differ, else for the whole milestone.

## Requirements *(mandatory)*

### Functional Requirements

#### Inputs & Preconditions

- **FR-001**: M6.1.1 MUST take M6.1's published JSON as a hard precondition input (default `docs/benchmarks/m6_1-real-prompt-embeds.json`, overridable via `--m6_1_1-m6-1-baseline`). If the file is missing, unreadable, or fails schema validation against `schema_version=="m6_1.v1"`, the harness MUST exit with code `1` and a stderr message naming the file and the override flag. The check MUST run BEFORE any Modal deployment is initiated.
- **FR-002**: M6.1.1 MUST use identical hardware/model configuration to M6.1: Modal A10G in `eu-west-1` (overridable via `--m6_1_1-modal-region`), `Qwen/Qwen3-8B` fp16, `enable_prompt_embeds=True` engine path, `max_model_len=2048`, `gpu_memory_utilization=0.92`. The harness MUST emit a methodology section in the published report confirming each value matches M6.1's published `run_meta`.
- **FR-003**: M6.1.1 MUST enforce the same `torch==2.11.0` pin M6.1 enforces (M6.1's FR-006). The pin check MUST run BEFORE M6.1 baseline loading and MUST exit with code `2` on mismatch. Identical exit-code semantics to M6.1.
- **FR-004**: M6.1.1 MUST refuse to run if M6.1's baseline file's `run_meta.engine_version` does not match the engine version M6.1.1 plans to deploy (`vllm==0.20.1` at time of writing). Mismatch MUST exit with code `1` and instruct the operator to either pin the engine to the matching version or pass an explicit acknowledgement flag (`--m6_1_1-allow-engine-mismatch`) which annotates the published report with the divergence.

#### Phase 1 — Diagnosis Mini-Sweep

- **FR-005**: Phase 1 MUST run 6 cells × 3 cohorts × n=50 measurement RPCs (900 total) — identical cell shape to M6.1, half the RPC count to keep wall-clock under ~30 min. Cells are `embed × {c=1, c=4, c=8}` and `chat_stream × {c=1, c=4, c=8}` at `hidden_size=4096`.
- **FR-006**: Phase 1 MUST emit four timing checkpoints on each chat_stream RPC, from both REST and gRPC paths: (a) `handler_entry` — the FastAPI handler / gRPC servicer receives the request; (b) `pre_engine` — just before `engine.generate(...)` is invoked; (c) `first_chunk` — the first streamed output chunk is produced; (d) `terminal_emit` — the terminal SSE event is emitted (REST) / trailing metadata is sent (gRPC). Each checkpoint is a `perf_counter_ns()` reading from the same local process clock.
- **FR-007**: REST chat_stream MUST emit the four timestamps as fields on a `m6_1_1_timings` sub-object on the terminal SSE event's JSON payload. The existing M6 SSE field set MUST be preserved exactly (no key changes, no order changes). The four fields are: `handler_entry_ns`, `pre_engine_ns`, `first_chunk_ns`, `terminal_emit_ns`.
- **FR-008**: gRPC chat_stream MUST emit the four timestamps as additional trailing-metadata keys, prefixed `m6_1_1_t_`: `m6_1_1_t_handler_entry`, `m6_1_1_t_pre_engine`, `m6_1_1_t_first_chunk`, `m6_1_1_t_terminal_emit`. Existing M6 trailing-metadata keys MUST be preserved unchanged.
- **FR-009**: Phase 1 MUST compute three per-segment deltas per RPC: `seg_ab_ms = (pre_engine − handler_entry) × 1e-6`, `seg_bc_ms = (first_chunk − pre_engine) × 1e-6`, `seg_cd_ms = (terminal_emit − first_chunk) × 1e-6`. Each segment is aggregated per cohort per cell (mean + 95% CI half-width via bootstrap n_boot=10_000, matching M6.1's confidence-interval methodology).
- **FR-010**: Phase 1 MUST classify each chat_stream cell with exactly one of four labels, deterministically derived from the per-segment data:
  - `instrumentation_artifact` — the per-cohort spread on `engine_ttft_ms` (M6.1's measurement) corresponds, segment-for-segment, to a spread within `seg_ab` (one path's pre-engine bracket includes work the other does not). Detection threshold: ≥80% of the M6.1-reported cell-level cohort spread maps onto `seg_ab`.
  - `channel_dependent_batching` — the M6.1 spread is reproduced inside `seg_bc` (engine itself sees different first-token latencies by cohort). Detection threshold: ≥80% of the M6.1-reported cell-level cohort spread maps onto `seg_bc`.
  - `drift_not_reproduced` — the Phase 1 mini-sweep itself shows per-cohort spread on `engine_ttft_ms` within 5% of the unweighted cohort-average for the cell (i.e., M6.1's reported drift is not reproduced at n=50).
  - `inconclusive` — none of the above thresholds are met (spread is distributed across multiple segments or below `drift_not_reproduced` only on some cohorts).
- **FR-011**: Phase 1 MUST also collect the four timestamps on embed cells as a control (no per-segment drift expected; M6.1's `engine_cost_drift_warning` did not fire on embed cells). The data is published in the Phase 1 report for audit but is NOT used to drive classification or the Phase 2 path decision.
- **FR-012**: Phase 1 MUST verify the instrumentation perturbation budget: total time spent in the four checkpoint reads per RPC MUST be less than 500 µs on average (per cohort per cell). If exceeded, the report emits a stderr warning and an `instrumentation_perturbation_warning` flag in the JSON, and the operator is asked to weight the Phase 1 numbers cautiously. (Mirrors M6.1's FR-022 `engine_cost_drift_warning` UX.)
- **FR-013**: Phase 1 MUST be driven by the CLI flag `--m6_1_1-diagnose` (mutually exclusive with `--m6_1_1`, `--m6_1_1-smoke`, `--m6_1`, `--m6`, `--m5_2`).

#### Phase 2 — Fix or Document

- **FR-014**: If Phase 1's classification across all three chat_stream cells is `instrumentation_artifact`, Phase 2(a) (code fix + verification sweep) applies. The operator applies the symmetrisation code change (path-specific; identified by Phase 1) and runs `--m6_1_1` (Phase 2(a) verification sweep). The verification sweep MUST run the same 6 cells × 3 cohorts × n=100 matrix M6.1 ran.
- **FR-015**: Phase 2(a) verification report MUST publish `engine_cost_drift_warning` status per cell (false expected on all three chat_stream cells if the fix is correct) and the new per-cohort `engine_ttft_ms` means with 95% CIs. Cleared iff every cohort's `engine_ttft_ms` mean is within 5% of the unweighted cohort-average for that cell.
- **FR-016**: If Phase 1's classification across all three chat_stream cells is `channel_dependent_batching`, Phase 2(b) (doc-only) applies. The operator updates `contracts/instrumentation.md` with a section describing the channel-dependent batching effect, its operator-facing interpretation, and how downstream milestones should read per-cohort `engine_ttft_ms` differences. No verification sweep is run.
- **FR-017**: If Phase 1's classification is mixed across the three chat_stream cells (e.g., two `instrumentation_artifact`, one `channel_dependent_batching`), Phase 2 path MUST be chosen by the operator after reviewing the Phase 1 report; the M6.1.1 spec does NOT auto-select a path. The chosen path MUST be recorded in the M6.1.1 JSON's `phase_2_choice` field along with the operator's brief justification (one sentence, plain text).
- **FR-018**: If Phase 1's classification is `inconclusive` or `drift_not_reproduced` for any cell, Phase 2 MUST NOT run automatically. The operator inspects the Phase 1 report and either re-runs Phase 1 or closes M6.1.1 with the corresponding annotation.

#### Reporting

- **FR-019**: M6.1.1 MUST publish markdown at `docs/benchmarks/m6_1_1-engine-cost-instrumentation.md` and JSON companion at `docs/benchmarks/m6_1_1-engine-cost-instrumentation.json`. The markdown MUST be human-readable without consulting the JSON; the JSON MUST be machine-readable without consulting the markdown.
- **FR-020**: The markdown report MUST contain the following sections in order: (1) Executive Summary (Phase 1 classification per chat_stream cell + Phase 2 path chosen + outcome); (2) Methodology (engine version, hardware, torch pin, baseline file, perturbation budget); (3) Multi-Point Timing Table (per cohort per cell, 4 checkpoints + 3 segment deltas, with 95% CIs); (4) Root-Cause Attribution (per chat_stream cell, narrative + segment-level evidence); (5) Phase 2 Outcome (verification numbers if Phase 2(a); doc-update reference if Phase 2(b)); (6) Methodology Supersedence (pointer from M6.1's JSON, see User Story 3).
- **FR-021**: The JSON companion MUST carry `schema_version="m6_1_1.v1"` and MUST be a strict superset of the M6.1 engine-cost-baseline schema. The top-level schema MUST include: `run_id`, `run_started_at`, `run_completed_at`, `run_meta` (matching M6.1's shape plus `phase_1_n` (=50) and `phase_2_path` (∈ {"phase_2a_verified", "phase_2b_documented", "phase_2_pending", "drift_not_reproduced", "inconclusive"})), `phase_1_classifications` (per-cell), `multi_point_timings` (per cohort per cell), `phase_2_outcome` (verification numbers OR doc-pointer), `m6_1_baseline_pointer`, and `methodology_supersedence`.
- **FR-022**: M6.1.1's JSON MUST be strict-superset compatible with M6.1-aware consumers: a consumer that reads M6.1's `engine_cost_baseline` section can read M6.1.1's `engine_cost_baseline` (under Phase 2(a) verification only) without breakage. Under Phase 2(b) the engine_cost_baseline section is omitted with a documented sentinel value.
- **FR-023**: M6.1.1 MUST annotate M6.1's published JSON (`docs/benchmarks/m6_1-real-prompt-embeds.json`) with a top-level `methodology_supersedence` field naming the M6.1.1 report path. The annotation is a small, additive write; M6.1's other fields MUST NOT change. The annotation is committed by the same PR that publishes M6.1.1's artifacts.
- **FR-024**: M6.1.1 MUST annotate M6.1's published markdown (`docs/benchmarks/m6_1-real-prompt-embeds.md`) with a one-line forward pointer to the M6.1.1 report in the chat_stream verdict section. The pointer is human-readable text; no machine-readable encoding is required at the markdown level.

#### CLI

- **FR-025**: `--m6_1_1-diagnose` runs Phase 1 (mini-sweep). `--m6_1_1` runs Phase 2(a) verification (n=100; only valid after a code fix has been applied). The two flags are mutually exclusive within a single invocation. Both flags are mutually exclusive with `--m6_1`, `--m6`, `--m5_2`, `--m5_1`, `--m5`, `--m4`, `--m3`.
- **FR-026**: `--m6_1_1-modal-region` defaults to `eu-west-1`; `--m6_1_1-modal-token-env` defaults to `MODAL_BENCH_TOKEN`; `--m6_1_1-modal-endpoint` accepts the `grpc=tcp+plaintext://...,rest_https_edge=https://...` form; `--m6_1_1-skip-deploy` requires `--m6_1_1-modal-endpoint`. Matches M6.1's CLI surface for the equivalent flags (FR-024 in M6.1).
- **FR-027**: `--m6_1_1-base-seed` defaults to `42` (matches M6 / M6.1). `--m6_1_1-model` defaults to `Qwen/Qwen3-8B`. `--m6_1_1-m6-1-baseline` defaults to `docs/benchmarks/m6_1-real-prompt-embeds.json`. `--m6_1_1-report-out` defaults to `docs/benchmarks/m6_1_1-engine-cost-instrumentation.md`. `--m6_1_1-report-json-out` defaults to `docs/benchmarks/m6_1_1-engine-cost-instrumentation.json`.
- **FR-028**: A smoke gate is NOT in scope for M6.1.1 (the Phase 1 mini-sweep at n=50 is itself the smoke-equivalent wiring check; running a smaller smoke ahead of n=50 adds complexity without commensurate confidence). The operator validates wiring by running the Phase 1 mini-sweep against a single cell (`--m6_1_1-diagnose --m6_1_1-cells embed:c=1` or similar) if needed; this is operator discretion, not a required harness mode.

#### Constraints & Out-of-Scope Boundaries

- **FR-029**: M6.1.1 MUST NOT re-compute M6.1's `supersedes_m6_under_enable_prompt_embeds` verdicts. The verdict table M6.1 published remains the published verdict table. M6.1.1 is a differential report on instrumentation methodology only.
- **FR-030**: M6.1.1 MUST NOT modify M6.1's CLI flags, M6.1's published artifacts beyond the additive `methodology_supersedence` annotation (FR-023, FR-024), or the M6.1 harness code paths except for the four-checkpoint instrumentation additions (and, under Phase 2(a), the bracketing fix).
- **FR-031**: Embed cells' `engine_forward_ms` measurement is out of scope for M6.1.1. M6.1's `engine_cost_drift_warning` did not fire on embed cells; the drift was isolated to chat_stream's `engine_ttft_ms`. M6.1.1's Phase 1 collects embed timestamps as a control (FR-011) but does not classify embed cells.
- **FR-032**: The `max_tokens` axis is out of scope (deferred to M6.2 — Phase Discipline). M6.1.1 measures at M6.1's fixed `max_tokens=10` (embed) / `max_tokens=50` (chat_stream).
- **FR-033**: Corpus diversity (M7) and multi-model (M8) are out of scope.
- **FR-034**: Smoke-gate code path (M6.1's FR-012) is unchanged. M6.1.1 does not add a smoke mode for the new instrumentation.

### Key Entities

- **M6.1.1 Phase 1 Diagnostic Run**: The result of one `--m6_1_1-diagnose` invocation. Contains 4-checkpoint timing data per cohort per cell (with embed cells as audit-only controls), per-segment delta aggregates, per-chat_stream-cell classification, and the perturbation-budget audit. Persisted to the JSON companion under `multi_point_timings` and `phase_1_classifications`.
- **Timing Checkpoint**: One of four named per-RPC timestamps (`handler_entry`, `pre_engine`, `first_chunk`, `terminal_emit`). Recorded as `perf_counter_ns()` on the local server process. Emitted on the wire (REST: `m6_1_1_timings` sub-object on terminal SSE event; gRPC: `m6_1_1_t_*` trailing metadata keys).
- **Per-Segment Delta**: One of three named per-RPC durations (`seg_ab_ms`, `seg_bc_ms`, `seg_cd_ms`), each derived from a consecutive checkpoint pair. Aggregated per cohort per cell.
- **Phase 1 Classification**: A per-cell label in `{instrumentation_artifact, channel_dependent_batching, inconclusive, drift_not_reproduced}`. Determined deterministically from per-segment data (FR-010 thresholds). Drives Phase 2 path selection (or the decision NOT to run Phase 2).
- **Phase 2(a) Verification Run**: The result of one `--m6_1_1` invocation, run after a symmetrisation code fix has been applied. Same matrix shape as M6.1 (6 cells × 3 cohorts × n=100). Publishes drift-cleared/persistent status per cell and new per-cohort `engine_ttft_ms` numbers.
- **Phase 2(b) Documentation Outcome**: A doc-only result containing the path to the updated `contracts/instrumentation.md` and a summary of the channel-dependent-batching finding. No verification sweep runs.
- **Methodology Supersedence Note**: An additive annotation written to both M6.1's JSON (top-level `methodology_supersedence` key) and M6.1's markdown (one-line forward pointer) directing readers to M6.1.1's published report. Committed by the same PR that publishes M6.1.1.

## Success Criteria *(mandatory)*

### Measurable Outcomes

- **SC-001**: After M6.1.1 Phase 1 completes, each of the three chat_stream cells has a single deterministically-derived classification label in `{instrumentation_artifact, channel_dependent_batching, inconclusive, drift_not_reproduced}` published in both the markdown and JSON reports.
- **SC-002**: M6.1.1 Phase 1 wall-clock on Modal A10G `eu-west-1` is under 45 minutes (target ~30 min per PLAN.md, with 50% slack for queue/cold-start variance).
- **SC-003**: If Phase 2(a) verification runs, the n=100 sweep shows `engine_cost_drift_warning=false` on all three chat_stream cells, with each cohort's `engine_ttft_ms` mean within 5% of the unweighted cohort-average for that cell (cleared from M6.1's 14–17% spread).
- **SC-004**: If Phase 2(b) documentation outcome applies, `contracts/instrumentation.md` carries a new section explaining the channel-dependent-batching effect at a level a reader unfamiliar with M6.1 can apply to interpret per-cohort `engine_ttft_ms` differences in published per-cohort engine-cost decompositions.
- **SC-005**: M6.1.1's published JSON is a strict superset of M6.1's `engine_cost_baseline` schema (M6.1-aware consumers can read M6.1.1's verification output without breakage; Phase 2(b) substitutes a documented sentinel).
- **SC-006**: M6.1's published JSON gains a top-level `methodology_supersedence` field naming the M6.1.1 report, and M6.1's published markdown gains a one-line forward pointer in the chat_stream verdict section. Both annotations are committed by the same PR that publishes M6.1.1.
- **SC-007**: Phase 1's total Modal compute cost stays under $2 (PLAN.md projection). Phase 2(a) verification cost stays under $1.50 (≈ M6.1 sweep cost; same matrix shape).
- **SC-008**: M6.2 (next milestone) can use M6.1's per-cohort `engine_ttft_ms` means and CIs as a null-anchor reference without ambiguity — either because Phase 2(a) symmetrised the measurement window (M6.1's numbers are still the right reference, with updated CIs from Phase 2(a)'s verification publishing) or because Phase 2(b) documented the channel-dependent-batching interpretation (M6.1's numbers are the right reference subject to the documented per-cohort interpretation rule).
- **SC-009**: Phase 1's instrumentation perturbation per RPC stays under 500 µs on average (FR-012 budget). The perturbation audit is published in the Phase 1 report's methodology section.
- **SC-010**: The decision between Phase 2(a) and Phase 2(b) is data-driven: a reader can reconstruct the classification and the chosen Phase 2 path solely from the Phase 1 multi-point timing data and the FR-010 thresholds, without consulting operator narrative.

## Assumptions

- M6.1's published JSON (`docs/benchmarks/m6_1-real-prompt-embeds.json` at the time of M6.1.1's spec drafting) is the immutable input for comparison; per-cohort `engine_ttft_ms` means and CIs in that file are the baseline M6.1.1 diagnoses against. M6.1.1 adds the `methodology_supersedence` annotation but does not modify any other field.
- Modal A10G `eu-west-1` availability and scheduler behavior is approximately stable run-to-run (M6 and M6.1 used the same region and reported consistent results). If a Phase 1 mini-sweep shows wildly different cohort means from M6.1's, the most likely explanation is M6.1.1's instrumentation perturbation, not infrastructure drift; the perturbation budget audit (FR-012) addresses this.
- vLLM 0.20.1's continuous batching behavior at `max_tokens=50`, `c ∈ {1, 4, 8}` is approximately deterministic enough that the per-cohort `engine_ttft_ms` spread observed in M6.1 reproduces at n=50 (Phase 1's sample size) above the `drift_not_reproduced` threshold. If it does not, that itself is the finding (`drift_not_reproduced` classification).
- The four timing checkpoints are descriptive of the `engine_ttft_ms` window in both REST and gRPC paths at a level sufficient to localize the M6.1 spread to one segment. If implementation discovers that a path's internal flow needs a fifth checkpoint, the additional checkpoint is added during `/speckit-plan` or `/speckit-tasks` with the wire-emission contract growing to match (the 4-key minimum in FR-007 / FR-008 is a floor, not a ceiling).
- The two-hypothesis dichotomy (`instrumentation_artifact` vs `channel_dependent_batching`) is the operationally relevant outcome dimension; `inconclusive` and `drift_not_reproduced` are documented fallbacks but the project expectation is that Phase 1 returns one of the two primary classifications on at least 2 of the 3 chat_stream cells.
- M6.1's `chat_stream_control_drift_warning` (FR-029 in M6.1) is unrelated to `engine_cost_drift_warning` (FR-022 in M6.1) — they detect different things. M6.1.1 only addresses `engine_cost_drift_warning`; the `chat_stream_control_drift_warning` flag's mechanism remains as M6.1 defined it. (Phase 2(a) verification may incidentally fire it; the report explicitly distinguishes the two flags.)
- `torch==2.11.0` continues to be the right client-side pin (M6.1's FR-006). If the project updates the pin between M6.1 and M6.1.1 publishing, M6.1.1's pin gate is updated to match in the same PR.
- The default Modal region (`eu-west-1`) is the right region for the verification sweep to match M6.1's network-stack characteristics. Switching regions between M6.1 and M6.1.1 would itself introduce a confounder; the spec defaults match M6.1 deliberately.
- The operator runs Phase 1, inspects the report, manually applies any code change (Phase 2(a)) or doc change (Phase 2(b)), and then runs Phase 2(a) verification if applicable. The harness does NOT auto-edit code or auto-apply doc changes; the human-in-the-loop step between Phase 1 and Phase 2 is intentional (the diagnosis informs which path to take, and the operator commits to it).
