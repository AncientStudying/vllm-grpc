---
description: "Task list for v0.0.1 — Bench-harness refactor"
---

# Tasks: v0.0.1 — Bench-harness refactor

**Input**: Design documents from `specs/029-post-m6.2-cleanup-v0.0.1/`
**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/cli-surface.md, contracts/module-api.md, quickstart.md

**Tests**: No new TDD suite requested. The existing `pytest` suite is the regression gate (FR-012); test work here is **deletion** (legacy), **rename** (retained), and **new-home coverage** (FR-007, data-model Entity 8).

**Scope**: All paths under `tools/benchmark/` unless noted. `SRC = tools/benchmark/src/vllm_grpc_bench`, `TST = tools/benchmark/tests`. **Forward-only / BC-breaking** — recovery via `milestone/m*` tags.

**Gate discipline (FR-008)**: every task that changes code ends with `cd tools/benchmark && ruff check . && mypy --strict .` green before the next. Full `make lint typecheck test` at story checkpoints.

---

## Phase 1: Setup

**Purpose**: Capture the pre-refactor baseline and confirm the recovery net.

- [X] T001 Confirm branch `chore/post-m6.2-cleanup-v0.0.1` is checked out and capture the green pre-refactor baseline. **DONE 2026-05-29**: `ruff check` clean; `mypy --strict tools/benchmark/src` → 84 files, no issues; `pytest tools/benchmark/tests` → **1457 passed, 2 skipped**. Baseline for SC-005 no-regression.
- [X] T002 Verify the recovery net. **DONE 2026-05-29**: `git tag | grep -c '^milestone/'` → 16; `git show milestone/m5.2-transport-tuning:tools/benchmark/src/vllm_grpc_bench/m5_2_sweep.py` returns content.
- [X] T003 Snapshot starting counts for SC-002. **DONE 2026-05-29**: 84 src modules, 137 test files.

**Checkpoint**: Baseline green, recovery net confirmed.

---

## Phase 2: Foundational (Generic homes — BLOCKS US1 and US2)

**Purpose**: Create the four milestone-agnostic homes with hoisted symbols. Nothing imports them yet (additive), so each lands green in isolation. Every later phase depends on these (research.md R7 step 1; data-model Entity 1).

**⚠️ CRITICAL**: No de-prefix or deletion work begins until this phase is complete.

- [X] T004 [P] Create `$SRC/exceptions.py` with `SchemaValidationFailed` (facade: re-exports `m5_2_regen.M5_2SchemaValidationFailed`; real def moves here at Phase 4). **DONE 2026-05-29**.
- [X] T005 [P] Create `$SRC/timing.py` (de-prefixed `m6_1_1_timing` surface: `extract_grpc_timings`, `extract_rest_timings`, `compute_per_segment_delta`, `timing_checkpoint_to_payload`, `TimingCheckpoint`, `PerSegmentDelta`). **DONE 2026-05-29**.
- [X] T006 Create `$SRC/types.py` with the non-colliding core (facade re-exports): `CohortKind` (4-member), `COHORTS`, `cohorts_at_concurrency`, `CELLS`, `Cell`, `Path`, `Path_`, `RTTRecord`, `EndpointTuple`, `RunCohort`, `RPCResult`, `RESTCohortRecord`, `RestHttpsEdgeCohortRecord`, `NetworkPath` (transport Literal), `CloudProvider`, `CohortOmissions`. **DONE 2026-05-29**. NOTE: the network-probe dataclasses (`M6_1_2NetworkPath`/`Hop`/`Error`) are deferred to `network_probe.py` (Phase 3 T014) to avoid colliding with the `NetworkPath` literal.
- [X] T007 Create `$SRC/prompts.py` with the unified seed+digest `build_chat_prompt` (facade: aliases `m6_rpc_driver._build_chat_prompt`) + `DEFAULT_CHAT_MAX_TOKENS`. **DONE 2026-05-29**. (rest_cohort repoint / M5.2-builder drop is Phase 3 T009; prompt_source merge is Phase 3.)
- [X] T008 Move `_client_kwargs` from `m3_sweep` into `$SRC/channel_config.py` (real move, not facade — avoids a channel_config↔m3_sweep cycle); repointed all 4 importers (`m5_1_grpc_cohort`, `m6_rpc_driver`, `m6_1_rpc_driver`, `m6_2_rpc_driver`) + `m3_sweep` itself at the new home. **DONE 2026-05-29**.

**Checkpoint**: Four homes exist and type-check; gate green (ruff + mypy --strict 88 files + pytest 1457 passed / 2 skipped, no regression vs baseline). Consumers not yet repointed (Phase 3) except the `_client_kwargs` importers.

---

## Phase 3: User Story 1 — Single milestone-agnostic codebase (Priority: P1) 🎯 MVP

**Goal**: The live harness imports only from generic homes / de-prefixed modules; one prompt builder, one reporter; no milestone-prefixed module name or import remains in live code.

**Independent Test**: grep live modules → zero imports of `m[0-9]` modules; `prompts.py` has one builder; one `*reporter*.py`; `make lint typecheck test` green; `python -m vllm_grpc_bench --validate --skip-deploy` completes (SC-003, SC-004, SC-006).

- [X] T009 [US1] Repoint `$SRC/rest_cohort.py` at `prompts.build_chat_prompt` (unified) + `types`/`timing` homes; remove the M5.2 prompt path (BC break, FR-003). Gate green. **DONE 2026-05-29**: imports repointed (`m3_sweep`→`prompts`, `m3_types`→`types`, lazy `m6_1_1_timing`→`timing`); synthetic-fallback path now calls `build_chat_prompt(i)` (seed-keyed) — drops the M5.2 `iter/cell_id` format; `cell_id` param kept vestigial (legacy `m5_2_sweep` still passes it; removed Phase 4). Rewrote `test_chat_payload_parity.py` to lock the unified contract (dropped 2 legacy `m3_sweep._build_chat_request` gRPC-builder assertions — covered forward by `test_m6_2_segment_aggregation`) and fixed `test_chat_corpus_parity` synthetic-fallback assertion. Gate (repo-root CWD): ruff clean, mypy --strict 88 files clean, pytest **1455 passed / 2 skipped** (= baseline 1457 − 2 dropped legacy tests, zero regression).
- [X] T010 [P] [US1] Repoint `$SRC/modal_endpoint.py`, `$SRC/rtt_probe.py`, `$SRC/ttft.py` at `types`. Gate green. **DONE 2026-05-29**: each had one `from vllm_grpc_bench.m3_types import X` (`EndpointTuple`/`RTTRecord`/`RunCohort`) repointed to the `types` home. Gate: ruff clean, mypy --strict 88 files clean, pytest 1455 passed / 2 skipped.
- [~] T010a [US1] **RE-SCOPED → Phase 4 (T020a) — 2026-05-29.** Consumer analysis (T010a discovery) showed the premise was wrong: `symmetric_prompts.py`'s lone `m3_types.M5_2CohortKind` import and the `tuned_grpc_channels`/`tuned_grpc` branches (lines ~209/230/321/336) live **entirely inside the dead M5.2 symmetry machinery** (`build_symmetry_block`, `assert_symmetry`, + the 5 dataclasses `CrossCohortInvariants`/`IntraProtocolPairInvariants`/`PerCohortMetadata`/`SymmetryBlock`/`CohortConfigInput`). That machinery's ONLY consumers are `m5_2_sweep.py` (which constructs `CohortConfigInput(cohort="tuned_grpc_channels")` at :1102 and `cohort="tuned_grpc"` at :1115), `m5_2_regen.py`, `test_m5_2_symmetry.py`, and `test_m5_2_types.py` — all deleted in Phase 4. The forward survivors (`assign_symmetric_prompt(cohort: str, …)`, `validate_symmetric_invariant`, `SymmetryAssertionFailed`) never reference `CohortKind` and are already milestone-agnostic; `test_m6_1_3_symmetric_prompts.py` is already 4-member-clean (no tuned-pair assertions to update). Repointing/stripping in Phase 3 would break the gate (mypy errors at `m5_2_sweep:1102/1115`, broken `test_m5_2_symmetry` assertions) and would violate the hard rule "no legacy deletion during US1." **The strip + de-import therefore moves to Phase 4 T020a**, executed in the same commit that deletes the M5.2 consumers. Nothing to safely do in Phase 3.
- [X] T011 [US1] De-prefix `m6_2_sweep`→`$SRC/sweep.py`: rename + repoint at homes. **DONE 2026-05-29.** `git mv m6_2_sweep.py sweep.py`; repointed `m6_1_2_types`/`m6_1_types` imports → `types` home (body de-prefixed: `M6_1_2_COHORTS`→`COHORTS`, `M6_1_2CohortKind`→`CohortKind`, `M6_1_CELLS`→`CELLS`; `M6_1Path` kept as alias for `types.Path` to dodge the `pathlib.Path` collision). Repointed all importers (`m6_2_reporter`, `m6_2_validate`, `m6_2_sub_probe` + 8 test files + the `from vllm_grpc_bench import m6_2_sweep` form in `test_m6_2_preemption_recovery`) and stale docstring refs `m6_2_sweep`→`sweep` — **protected the out-of-scope `scripts/python/monitor_m6_2_sweep.py` references** via negative lookbehind. Sibling `m6_2_*` imports (`m6_2_anchor_trajectory`, `m6_2_prompt_source`, `m6_2_types`) left for their own de-prefix tasks. **seq_len fold-in re-scoped** (see T014a): `m6_2_sweep` never used `pin_seq_len_at_sweep_start` (the live caller is `m6_2_validate`); per user decision `m6_1_seq_len` becomes its own de-prefixed home, not absorbed into sweep. Gate: ruff clean, mypy --strict 88 files clean, pytest 1455 passing (1 load-sensitive timing flake in `network_probe`, unrelated, passes on retry).
- [X] T012 [US1] De-prefix `m6_2_rpc_driver`→`$SRC/rpc_driver.py`: rename, repoint at homes, and absorb the live helpers from `m6_rpc_driver` (`_build_chat_grpc_request`, `_build_chat_rest_payload`, `_rest_rtt_probe`) and `m6_1_rpc_driver` (`_build_embed_grpc_request`, `_build_embed_rest_payload_m6_1`, `_normalize_rest_url_for_httpx`, `_resolve_rpc_index`); import `parse_grpc_trailing_metadata`, `parse_rest_response` from the de-prefixed `engine_cost` module (T013) rather than absorbing them. Gate green. **DONE 2026-05-30** (after T013 prerequisite). `git mv m6_2_rpc_driver.py rpc_driver.py`; repointed imports at homes (`m3_types`→`types` RTTRecord; `m6_1_2_types.M6_1_2CohortKind`→`types.CohortKind`; `m6_1_types.M6_1Cell`→`types.Cell`; `m6_sweep.RPCResult`→`types.RPCResult`; chat-builder default `M6_CHAT_MAX_TOKENS`→`prompts.DEFAULT_CHAT_MAX_TOKENS` + prompt fallback→`prompts.build_chat_prompt`; 4 inline `m6_1_1_timing`→`timing`). Absorbed all 7 listed helpers. **PLAN GAP (discovered)**: the absorbed embed builders transitively need `build_torch_save_bytes`→`build_torch_generator_for_rpc` (defined in `m6_1_seed`, deleted Phase 4), which T012's list omitted; absorbed both as private `_build_torch_save_bytes`/`_build_torch_generator_for_rpc` so `rpc_driver` is self-contained for the embed path. `m6_1_seed`'s other symbols (`compute_rpc_seed`, `DEFAULT_M6_1_BASE_SEED`) have **no live consumers** → `m6_1_seed` becomes fully orphaned, deleted in T020 (no separate home needed). Repointed importers: `m6_2_validate.py` (live import), `test_m6_2_rest_driver_timing` (import), `test_m6_2_preemption_recovery` (4 mocker.patch string targets), + de-prefixed docstring module refs in `m6_2_anchor_trajectory`/`test_m6_2_modal_integration` (used word-boundary `\bm6_2_rpc_driver\b` to avoid mangling the kept `provide_m6_2_rpc_driver` symbol). Symbol names (`provide_m6_2_rpc_driver`, `M6_2RPCDriver`, `_drive_*_m6_2`, `_build_embed_rest_payload_m6_1`) left milestone-flavored (T028a); reworded docstring/comment prose to drop `from m6_*` patterns that would trip the T019 grep. Gate: ruff clean, mypy --strict 87 files clean, pytest **1455 passed / 2 skipped** (= baseline, zero regression).
- [X] T013 [US1] De-prefix `m6_engine_cost`→`$SRC/engine_cost.py` (rename + repoint at homes); it is the single home for the cost parsers consumed by `rpc_driver` (no double-move into `rpc_driver`/`metrics`). Gate green. **DONE 2026-05-30** (sequenced before T012, which imports the cost parsers from this de-prefixed home). `git mv m6_engine_cost.py engine_cost.py`; repointed all importers (`m6_1_sweep`, `m6_sweep`, `m6_supersede`, `m6_2_rpc_driver`, `m6_1_rpc_driver`, `m6_rpc_driver` + 6 legacy test files) `m6_engine_cost`→`engine_cost`. **Cycle discovery + structural fix**: initially routed the cost types (`EngineCostSpan`/`EngineCostAggregate`/`M6Path`/`M6CohortKind`/`M6_DRIFT_WARNING_PCT`) through the `types` home, which created `engine_cost → types → m6_sweep → engine_cost` (the home re-exports `RPCResult` from `m6_sweep`, which imports `aggregate_engine_cost_per_cell` from here). Resolved by making `engine_cost.py` the **leaf home** that *owns* the two cost dataclasses (imports only `ci`); `m6_types` now re-exports `EngineCostSpan`/`EngineCostAggregate` **from** `engine_cost` (single nominal type preserved for `RPCResult.engine_cost`). `M6Path`/`M6CohortKind` are structural `Literal`s (duplicated defs are type-identical); `M6_DRIFT_WARNING_PCT` is a local constant. `M6`-flavored symbol names retained (T028a). Gate: ruff clean, mypy --strict 87 files clean, pytest **1455 passed / 2 skipped** (= baseline, zero regression). **Flagged + RESOLVED 2026-05-30** (commit `00a07ac`): pre-existing `ruff format` drift in `sweep.py` (two over-length lines committed in T011, unrelated to T013) tripped `make lint`'s `ruff format --check`; folded in a cosmetic-only reformat so `make lint` is fully green ahead of the T019 US1 checkpoint.
- [X] T014 [US1] De-prefix `m6_1_2_network_probe`→`$SRC/network_probe.py`; repoint at `types`. Gate green. **DONE 2026-05-30.** `git mv`; repointed `network_probe`'s lone `m6_1_2_types` import → `types` home (de-prefixed body `M6_1_2_COHORTS`→`COHORTS`, `M6_1_2CohortKind`→`CohortKind`, `M6_1_2CloudProvider`→`CloudProvider`; network-probe dataclass names `M6_1_2NetworkPath`/`Hop`/`Error` kept — T028a). **Network-probe dataclass home decision** (reconciles the T006 deferral note vs data-model Entity 1): used the **RPCResult facade pattern** — the 3 dataclasses' definitions stay in `m6_1_2_types` (a near-leaf) and the `types` home **re-exports** them; `network_probe` (+ `sweep_types`, the T015pre-deferred consumer) resolve them from the `types` home. This is cycle-free (`network_probe → types → m6_1_2_types → m6_1_types`, no back-edge) — a network_probe-owned home would have created `network_probe → types → m6_1_2_types → network_probe`. No name clash with the `NetworkPath` transport literal while prefixed names persist; the physical move + de-prefix + clash-resolution happen at Phase 4 / T028a. Kept `M6_1_2*` names means no collision now. Repointed module importers (`sweep.py`, `m6_2_validate` live; `m6_1_2_sweep`/`m6_1_3_sweep` legacy; `test_m6_1_2_network_probe` incl. 5 mocker.patch targets, `test_m6_1_2_progress_format`). Updated `types.py` docstring + `sweep_types` comment/`__all__` note. `m6_2_reporter`'s dataclass import left on `m6_1_2_types` (still defines them) → repointed in its own de-prefix task (T017). Gate: ruff clean, mypy --strict 87 files clean, pytest **1455 passed / 2 skipped** (= baseline, zero regression).
- [X] T014a [P] [US1] **(re-scoped from T011 — user decision 2026-05-29)** De-prefix `m6_1_seq_len.py`→`$SRC/seq_len.py` as its own cohesive home (90-line tokenizer-pinning unit with its own test). `git mv`; repoint the live consumer `validate.py` (T015) + the transitional legacy callers (`m6_1_1_sweep`, `m6_1_sweep`, `m6_1_3_validate`, `m6_1_2_validate` — deleted Phase 4) + `test_m6_1_2_cli.py` at `seq_len`. Rationale: `m6_2_sweep` never imported `pin_seq_len_at_sweep_start`; folding it into `sweep.py` (which doesn't call it) is a contrived merge (SC-002). `test_m6_1_seq_len.py`→`test_seq_len.py` rename is Phase 4 (T022). Must land before/with T015. Gate green. **DONE 2026-05-30.** `m6_1_seq_len.py` is a self-contained leaf (no `vllm_grpc_bench` imports — `pin_seq_len_at_sweep_start` + tokenizer cache + precomputed table only), so this was a pure `git mv` + reference repoint, no "repoint at homes" needed. Repointed all `m6_1_seq_len`→`seq_len` refs (word-boundary, so the `seq_len` int/param name is untouched): live consumer `m6_2_validate` (function-local import, becomes `validate.py` T015) + legacy callers `m6_1_sweep`/`m6_1_1_sweep` (import + docstring `:func:`)/`m6_1_3_validate`/`m6_1_2_validate` + the orphaned `m6_1_seed` docstring `:mod:` ref + `test_m6_1_2_cli` import + the dedicated `test_m6_1_seq_len.py` internals (`from vllm_grpc_bench import seq_len`, all `seq_len.*` accesses; **file kept as `test_m6_1_seq_len.py`** — renamed to `test_seq_len.py` in T022). Gate: ruff clean, mypy --strict 87 files clean, pytest **1455 passed / 2 skipped** (= baseline, zero regression).
- [X] T015pre [US1] **PLAN GAP found in T011 — RESOLVED 2026-05-29 (user decisions).** Two unscheduled live `m6_2_*` modules de-prefixed: **(1/2)** `m6_2_types`→`sweep_types.py` as its own sweep-domain types module (user chose own-module over folding into the `types` home — avoids a `types`↔`network_probe` cycle and keeps the shared home thin). Cohort/cloud symbols aliased from the `types` home (re-export names preserved for `m6_2_validate`); network-probe dataclasses stay on `m6_1_2_types` → T014. 22 importers repointed. **(2/2)** `m6_2_prompt_source`→**merged into `prompts.py`** (user chose merge per the T007 note; `prompts.py` is now the unified prompt-construction + corpus-source home, ~270 lines). `git rm m6_2_prompt_source.py`; 8 importers repointed; `_build_chat_prompt`→local `build_chat_prompt`. Module count **88→87** (first net reduction). `M6_2*`/`M6_1_2*` *symbol* names left intact (module de-prefix, not a symbol-rename pass — T019 grep is import-only; uppercase `M6_2` doesn't match). Gate (both parts): ruff + mypy --strict clean, pytest 1455 passed / 2 skipped.
- [X] T015 [US1] De-prefix `m6_2_validate`→`$SRC/validate.py`: rename, repoint at homes/`network_probe`/`sweep`, and **preserve the canonical/validate path constants verbatim** (`_CANONICAL_JSON/_MD`, `_VALIDATE_JSON/_MD`, FR-019, research.md R5). Gate green. **DONE 2026-05-30** (2091-line module; `seq_len`/`network_probe`/`rpc_driver` imports were already repointed in T014a/T014/T012). `git mv`; repointed the home/type imports (`m6_1_2_types`→`types` `COHORTS`/`CohortKind`; `m6_1_types`→`types` `CELLS`/`Cell`, incl. the function-local `Cell as _M6_1Cell` alias) and de-prefixed the 4 home symbols across the body (34 usages). **Preserved verbatim** (FR-019): the four `docs/benchmarks/m6_2-token-budget*.{md,json}` `_CANONICAL_*`/`_VALIDATE_*` constants. **CLI-arg vs module-name hazard**: the token `m6_2_validate` is *also* the CLI dest (`args.m6_2_validate`, `getattr(args,"m6_2_validate",…)`, `m6_2_validate=` kwargs, `test_m6_2_validate_*` names) — Entity 6, renamed in T018 — so I repointed **only the qualified module path** `vllm_grpc_bench.m6_2_validate`→`vllm_grpc_bench.validate` (unambiguous; never matches the arg) across `__main__` + 4 test files, and left every `args.m6_2_validate` intact. Cleaned the 9 `[m6_2_validate]` log prefixes → `[validate]` (verified no test asserts them) + the self-docstring ref. **Caught a de-prefix over-reach**: a function-local `from sweep_types import M6_1_2_COHORTS` got blanket-renamed to `COHORTS`, which `sweep_types` only re-exports under the aliased name — repointed that one to the `types` home (mypy caught it). Sibling `m6_2_*` imports (`crossover`, `null_anchor`, `anchor_trajectory`, `reporter`, `sub_probe`, `resume`) left for T016/T017 (T011 pattern); updated `sweep.py`'s bare `m6_2_validate.*` docstring refs (already-de-prefixed sibling, no arg refs). Gate: ruff clean, mypy --strict 87 files clean, pytest **1455 passed / 2 skipped** (= baseline, zero regression).
- [X] T016 [P] [US1] De-prefix the remaining live `m6_2_*` modules → `$SRC/{resume,crossover,null_anchor,anchor_trajectory,sub_probe}.py`; repoint each at homes. Gate green. **DONE 2026-05-30.** `git mv` all 5; repointed every module reference (imports, intra-batch deps `anchor_trajectory→null_anchor` / `sub_probe→crossover`, patch strings, docstrings) across src+tests + the not-yet-de-prefixed `m6_2_reporter`'s imports of these 5 (its own de-prefix is T017). De-prefixed cohort symbols (`M6_1_2_COHORTS`→`COHORTS`, `M6_1_2CohortKind`→`CohortKind`) and repointed `m6_1_2_types`→`types` home in all 5. **`m6_2_resume` is a dual token** (like `m6_2_validate`): it's *also* the CLI arg `args.m6_2_resume` (Entity 6, T018) AND the `RESUME_SCHEMA_VERSION="m6_2_resume.v1"` data string (+ test fixture `"m6_2_resume.v999"`) — all three are **quoted**, so I repointed only the qualified `vllm_grpc_bench.m6_2_resume` module path + the one bare docstring ref, leaving the arg and schema strings verbatim (the schema-version string is data, not a module import — deferred like other milestone-flavored strings; doesn't trip the anchored T019 grep). The other 4 tokens have no arg/string collisions → blanket renamed. **Tooling note**: a zsh `for f in $files` loop and macOS `xargs -0` both failed to split the file list (no partial edits leaked — verified `git diff` clean between attempts); `find … -exec perl -0pi … {} +` is the robust pattern here. **Result**: all 5 modules `m[0-9]`-import clean; `sweep.py` is now **fully clean** (its `anchor_trajectory`/`resume` imports repointed); `validate.py`'s only residual `m[0-9]` import is `m6_2_reporter` (clears in T017). Gate: ruff clean, mypy --strict 87 files clean, pytest **1455 passed / 2 skipped** (= baseline, zero regression).
- [~] T017 [US1] Consolidate report generation (FR-005): replace `$SRC/reporter.py` content with the de-prefixed `m6_2_reporter`; delete the M1-era functions (`write_summary_md`, `write_cross_run_md`, `write_wire_size_comparison_md`, M1 variants) and their M1-era `m3_types` imports (research.md R4). Verify `ls $SRC/*reporter*.py | wc -l` → will be 1 after Phase 4 deletes the six `m*_reporter.py`. Gate green. **PARTIAL — additive consolidation DONE 2026-05-30 (user decision); deletion deferred to Phase 4.** Discovered the M1-era *deletion* is blocked: the legacy `reporter.py` writers (`write_json`/`write_csv`/`write_summary_md`/`write_cross_run_md`/`write_three_way_md`/`write_m4_*`/`write_m5_*`/`write_m5_1_*`/`write_m5_2_*`) are still consumed by live `__main__` milestone CLI branches (removed in T018), the **retained** `test_reporter.py` (M1 writers), legacy `m5_2_regen` (T020), and legacy `test_m4_*`/`test_m5_*` (T021) — deleting now breaks the gate. **User chose "additive now, delete post-T018"** (vs reorder-T018-first / pull-T018-work-in). **DONE part**: home-cleaned `m6_2_reporter` (repointed `m6_1_2_types`/`m6_1_types`→`types`, de-prefixed `M6_1_2CohortKind`→`CohortKind`/`M6_1_CELLS`→`CELLS`), then **spliced its 1080-line body into `reporter.py`** under a `# === M6.2 report writers ===` marker (renamed the colliding legacy `_fmt`→`_fmt_legacy`, 11 sites; the M6.2 `_fmt` is the keeper — only collision, confirmed by `comm`), merged imports (+`Iterable`), repointed consumers (`validate` + `test_m6_2_artifact_schema`/`test_m6_2_validate_cli`/`test_m6_2_publish_cli`) `m6_2_reporter`→`reporter`, and `git rm`'d `m6_2_reporter.py` (module count 86). The legacy block sits under a `# === LEGACY M1–M5.2 report writers (deleted in the T017-tail after the T018 CLI strip) ===` marker. **Net**: `validate.py` is now fully `m[0-9]`-clean; `reporter.py` transitionally carries the legacy writers + their `m3_types` (line 16) + a function-local `m5_2_regen` import (in `write_m5_2_json`) until Phase 4. **Deferred T017-tail** (folded into T020/T022): delete the legacy `reporter.py` writers + the `m3_types`/`m5_2_regen` imports once `__main__` (T018), `m5_2_regen` (T020), and legacy tests (T021) are gone, and gut/repoint `test_reporter.py` (T022). Gate: ruff clean, mypy --strict 86 files clean, pytest **1455 passed / 2 skipped** (= baseline; 1 known load-sensitive `network_probe` timing flake passed on isolation + serial re-run).
- [X] T018 [US1] Strip the CLI in `$SRC/__main__.py` (FR-018a, contracts/cli-surface.md): remove every `--m3`…`--m6_1_3` flag group + `args.mN` dispatch branch; rename surviving `--m6_2-*`→generic; drop the `--m6_2` selector so the sweep is the default invocation. Repoint remaining imports at de-prefixed modules. Gate green. **DONE 2026-05-30 — with a spec reconciliation (ADR `docs/decisions/0006-cli-keep-bench-add-sweep.md`).** **Decision (user, "overwhelming goal is simplification")**: the literal "sweep is the *default* invocation" collided with the README/Makefile-documented `bench` (proxy-vs-native) no-arg default + `compare*` subcommands, which the spec *retains* (Entity 3 `runner`/`metrics`/`compare`, Entity 8 `test_runner`/`test_compare`/`test_reporter`). Resolved: **keep `bench` as the no-arg default + `compare*` subcommands; the de-prefixed sweep is a new `sweep` subcommand**. Removed all `--m3`…`--m6_1_3` flag groups + the ~15 `_run_mN`/`_validate_mN`/`_build_m4_config`/`_normalize_m6_1_3_*` functions + the milestone `main()` branches + the `TYPE_CHECKING` milestone imports (`__main__.py` **2906 → ~205 lines**). De-prefixed the `--m6_2-*` flags under the `sweep` subparser (`--modal-region`, `--n`, `--validate`, …) and **renamed the arg dests** `m6_2_modal_region`→`modal_region`, `m6_2_n`→`n`, `m6_2_validate`→`validate`, `m6_2_resume`→`resume`, … across `validate.py` + the 3 sweep CLI tests (this finally clears the `args.m6_2_validate`/`args.m6_2_resume` dests preserved in T015/T016). `_validate_m6_2_args`→`_validate_args` (dropped the milestone mutual-exclusion block, kept the validate n=20 pin). Rewrote `test_m6_2_cli.py` for the `sweep` subcommand + a new "zero `--m[0-9]` flags across main+subparsers" assertion (SC-009). **Partial-T021 pull-forward**: deleted the **15** legacy CLI/smoke test files that imported the removed `__main__` milestone helpers (`test_m4_cli`, `test_m4_cli_schema`, `test_m5{,_1,_2}_cli`, `test_m6{,_smoke}`, `test_m6_1{,_1,_2,_3}_cli`, `test_m6_1_3_{publish_multirun,validate}_cli`, `test_m3_cli_args`, `test_m3_sweep_smoke`). **Bench/compare untouched**: `make bench`/`bench-ci`/`bench-compare` still work; added `make sweep`/`sweep-validate` (Makefile — see T030 fence widening). **Caught a bug**: my first sweep-subparser splice was too greedy and swallowed the bench-default flags (`--proxy-url` etc., which sat right after the M6.2 group once M5.1/M5 were deleted) — recovered them from `git show HEAD`. Gate: ruff clean, mypy --strict 86 files clean, `--help`/`sweep --help` → 0 `--m[0-9]` flags, pytest **1280 passed / 2 skipped** (= prior 1455 − ~175 deleted legacy CLI tests, zero regression in kept tests).
- [X] T019 [US1] Verify US1 invariants (contracts/module-api.md I3/I4/I5). **The US1 grep gate carries a documented carve-out (see "Facade-home carve-out (T019)" in Notes) — it is NOT a literal zero across all modules, because the four Entity-1 facade homes still re-export from legacy until the Phase-4 hoist.** Run the **anchored** grep (matches real `import`/`from` statements only, so prose/comments like channel_config's "Hoisted from `m3_sweep`" and rest_cohort's "touching m6_1_1_timing" do not false-positive): `grep -rnE '^[[:space:]]*(from|import)[[:space:]]+[A-Za-z0-9_. ]*\bm[0-9]' $SRC --include='*.py'`. Expected residual = the four facade homes `types.py`, `prompts.py`, `timing.py`, `exceptions.py` **plus `reporter.py`** (see below — the additive T017 leaves the legacy M1–M5.2 writers + their `m3_types`/`m5_2_regen` imports in `reporter.py` until T020). **Assert zero in every other live module** — i.e. the de-prefixed functional modules `sweep`, `validate`, `rpc_driver`, `network_probe`, `engine_cost`, `seq_len`, the `m6_2_*` leaves (post-T016), `__main__` (post-T018), `sweep_types`, `rest_cohort`, `channel_config`, `modal_endpoint`, `runner`, `metrics`, etc. (`symmetric_prompts`'s last `m3_types` import clears in T020a). Also: `python -m vllm_grpc_bench --help | grep -cE '\-\-m[0-9]'` → 0; `python -m vllm_grpc_bench --validate --skip-deploy` completes; `make lint typecheck test` green. (The **strict** zero-across-ALL-modules form of I3 is the **T024** gate, after Phase 4 deletes the legacy sources and hoists the home definitions in-place.) **DONE 2026-05-30** (verification-only checkpoint, no source change). **Anchored grep (I3/I4)**: filtering the legacy `m[0-9]*`-named source files (deleted in T020) out of the result, the residual milestone imports in *live* modules are exactly the documented carve-out set + the one T020a pre-clear: `types.py` (m3_types/m6_1_2_types/m6_1_types/m6_sweep), `prompts.py` (m3_sweep/m6_rpc_driver), `timing.py` (m6_1_1_timing/m6_1_1_types), `exceptions.py` (m5_2_regen), `reporter.py` (m3_types line 16 + function-local m5_2_regen line 1579 — the LEGACY-marked writers), and `symmetric_prompts.py` (m3_types `M5_2CohortKind`, line 38 — the last import there, explicitly slated to clear in T020a). **Every functional de-prefixed module is clean** — `sweep`, `validate`, `rpc_driver`, `network_probe`, `engine_cost`, `seq_len`, the 5 `m6_2_*` leaves (`resume`/`crossover`/`null_anchor`/`anchor_trajectory`/`sub_probe`), `__main__`, `sweep_types`, `rest_cohort`, `channel_config`, `modal_endpoint`, `runner`, `metrics` → 0 residual. **SC-009**: `--help` → 0 `--m[0-9]` flags; `sweep --help` → 0; subcommands = `{compare,compare-cross,compare-three-way,sweep}` + no-arg `bench` default (ADR 0006). **FR-013/SC-006**: `python -m vllm_grpc_bench sweep --validate --skip-deploy` runs the fake-backed validate sweep end-to-end → exit 0 (66 blocks + 16-block subprobe; wrote `m6_2-token-budget-validate.{md,json}`). **I5 (checkable half)**: `CohortKind` = exactly 4 members (`rest_https_edge`,`rest_plain_tcp`,`default_grpc`,`tuned_grpc_multiplexed`) — SC-003; `build_chat_prompt`/`DEFAULT_CHAT_MAX_TOKENS` resolve from the `prompts` home (the single chat-prompt builder). (The "exactly one `*reporter*.py` module" half of I5 + the strict carve-out-free I3 are the **T024** gate — legacy `m6_*_reporter` sources are still on disk until T020.) **Gate**: `make lint` clean (ruff check + `ruff format --check` 273 files), `make typecheck` clean (`mypy --strict` 104 source files), `uv run pytest tools/benchmark/tests` **1280 passed / 2 skipped** (= post-T018 baseline, zero regression).

**Checkpoint**: Live harness is milestone-agnostic and green — legacy modules still on disk but no longer imported by live code.

---

## Phase 4: User Story 2 — Legacy gone, counts hit target (Priority: P2)

**Goal**: All milestone-prefixed source modules and tests deleted; retained tests renamed; counts toward ~25 src / ~35 test.

**Independent Test**: `ls $SRC | grep -cE '^m[0-9]'` → 0; `ls $TST | grep -cE '^test_m[0-9]'` → 0; `make lint typecheck test` green (SC-001, SC-002).

- [X] T020 [US2] **Hoist-then-delete.** FIRST hoist the four Entity-1 facade homes' real definitions **in-place** so they stop importing from legacy and survive the deletion (this is what dissolves the T019 carve-out — see Notes → "Facade-home carve-out"): move `RPCResult` (+ any other `m6_sweep`/`m3_types`/`m6_1_types`/`m6_1_2_types`-sourced defs) into `types.py`; the `m6_1_1_timing` + `m6_1_1_types` timing surface into `timing.py`; `DEFAULT_CHAT_MAX_TOKENS` (from `m3_sweep`) + `build_chat_prompt` (from `m6_rpc_driver`) into `prompts.py`; `SchemaValidationFailed` (from `m5_2_regen`) into `exceptions.py` — dropping the `from …mN…` re-export lines as each def lands. THEN `git rm` all now-orphaned legacy source modules: `$SRC/{m3_*,m4_*,m5_*,m5_1_*,m5_2_*}.py`, the `m6_*` family (`m6_sweep,m6_types,m6_reporter,m6_smoke,m6_seed,m6_supersede,m6_rpc_driver,m6_engine_cost` if fully merged), `m6_1_*` (incl. `m6_1_seed` — fully orphaned after T012/T014a), `m6_1_1_*` (except `timing`, already moved), `m6_1_2_*` (except `network_probe`, already moved), `m6_1_3_*`. Then `mypy --strict .` confirms zero dangling imports (this check FORCES the hoist — the homes break compile if any def is left behind). (SC-001 source half.) **Also (T017-tail, FR-005; NARROWED by ADR 0006):** delete the legacy report writers from `$SRC/reporter.py` that are *fully* orphaned once `__main__`'s milestone branches (T018), `m5_2_regen` (this task), and legacy tests (T021) are gone — i.e. `write_json`/`write_csv`/`write_summary_md` (M1), `write_m4_*`/`write_m5_*`/`write_m5_1_*`/`write_m5_2_*`, plus the `m3_types`/`m5_2_regen` imports they pulled. **KEEP `write_cross_run_md` + `write_three_way_md`** — the retained `compare-cross`/`compare-three-way` subcommands still call them (ADR 0006). Drop `_fmt_legacy` only if no surviving writer uses it. Net: `reporter.py` is `m[0-9]`-clean (the kept `write_cross_run_md`/`write_three_way_md` take their cohort/run types from `metrics`, not `m3_types`). **DONE 2026-05-30** (with T020a, same commit). **Hoist**: the 4 facade homes are now self-contained — `types.py` owns `RPCResult` + the 4-member `CohortKind`/`COHORTS`/`CloudProvider`/`CohortOmissions`/`cohorts_at_concurrency` + the 3 `M6_1_2NetworkPath*` dataclasses + `Cell`/`CELLS`/`Path` + the M3 cohort web (`RunCohort`/`Sample`/`BenchmarkCell`/`ExpansionRecord`/`RTTRecord`/`RESTCohortRecord`/`RestHttpsEdgeCohortRecord`/`EndpointTuple`/`NetworkPath`/`Path_`), importing only `grpc`/`channel_config`/`engine_cost`; `timing.py` owns `TimingCheckpoint`/`PerSegmentDelta` + the 4 extractors; `prompts.py` owns `build_chat_prompt` + `DEFAULT_CHAT_MAX_TOKENS`; `exceptions.py` owns `SchemaValidationFailed`. **Deleted** all 52 `m[0-9]*.py` legacy source modules → `ls $SRC | grep -cE '^m[0-9]'` = **0** (SC-001 source half). `mypy --strict` (the forcing function) **green, 53 files** — confirms zero dangling imports + correct hoist. **TWO PLAN-GAP CORRECTIONS (ADR 0007, mypy-surfaced):** (1) **`m3_sweep` was NOT pure legacy** — it held the LIVE `M3ChatServicer`/`M3CompletionsServicer` + `serve_in_process[_adapter]` deployed by the out-of-fence Modal bench scripts (reachable from retained `modal_endpoint.py`). Per user decision: de-prefixed them into a NEW retained `grpc_servicers.py` (`ChatServicer`/`CompletionsServicer`), repointed the 2 Modal scripts + `test_endpoint_provider` (its 3rd `_measure_cell`/M4 case excised — covered deleted code), and **widened the FR-018 fence** to the 2 bench scripts (ADR 0007, extending ADR 0006's Makefile precedent). (2) **the M1 `bench` reporter writers are LIVE** — `write_json`/`write_csv`/`write_summary_md` serve the retained `bench` no-arg path (ADR 0006), so they were KEPT (not deleted as the task text said); only the truly-orphaned M4/M5/M5.1/M5.2 writers + `write_wire_size_comparison_md` + their `m3_types`/`m5_2_regen` imports were removed (`reporter.py` 2830→1380 lines, `m[0-9]`-clean). `m6_1_torch_pin` confirmed orphaned → deleted (resolves part of T028). **Gate**: `make lint` clean (ruff check + format 222 files), `make typecheck` clean (`mypy --strict` 53 files), end-to-end `sweep --validate --skip-deploy` smoke green through all 4 homes + reporter; `test_chat_payload_parity` + `test_endpoint_provider` pass (8). **Full `make test` is deferred** — legacy `test_m[0-9]_*` + 4 retained-name tests (`test_rtt_probe`/`test_mock_engine`/`test_chat_corpus_parity` importing `m3_types`/`m5_1_grpc_cohort`) still reference deleted modules → repointed/deleted in T021/T022 (per the Phase-4 decomposition; pytest goes green at the post-T024 checkpoint).
- [X] T020a [US2] **(re-scoped from T010a)** In the SAME commit as T020's `m5_2_*` deletion, strip the now-orphaned M5.2 symmetry machinery from the kept `$SRC/symmetric_prompts.py`: remove `build_symmetry_block`, `assert_symmetry`, the 5 dataclasses (`CrossCohortInvariants`, `IntraProtocolPairInvariants`, `PerCohortMetadata`, `SymmetryBlock`, `CohortConfigInput`), the helpers used only by them (`canonical_digest`, `_digest_excepting`), and the `from vllm_grpc_bench.m3_types import M5_2CohortKind` import (the last `m[0-9]` import in this module — needed for the T019/T024 grep gate). **Preserve** the forward survivors `assign_symmetric_prompt`, `validate_symmetric_invariant`, and `SymmetryAssertionFailed` (still raised by `validate_symmetric_invariant` and imported by `test_m6_1_3_symmetric_prompts.py`). Update the module docstring (drop the M5.2 tier-(a)/(b)/(c) description). Ordering: this MUST land with (or after) the deletion of `m5_2_sweep`/`m5_2_regen`/`m5_2_symmetry` + `test_m5_2_symmetry`/`test_m5_2_types`, since those are the only consumers of the removed machinery. Gate green. **DONE 2026-05-30** (same commit as T020). `symmetric_prompts.py` rewritten to keep only `SymmetryAssertionFailed`, `assign_symmetric_prompt`, `validate_symmetric_invariant`; dropped the `m3_types` import + the `hashlib`/`dataclass`/`field`/`Any` imports that only the removed machinery used (kept `json` for `validate_symmetric_invariant`). It is now `m[0-9]`-import-clean. `make lint`/`typecheck` green.
- [X] T021 [US2] `git rm` all legacy test files: `$TST/{test_m3_*,test_m4_*,test_m5*,test_m6_*,test_m6_1*}.py` (NOT `test_m6_2_*`). Do **not** delete `test_modal_endpoint_m5_2.py` — it covers the retained dual-transport (`with_rest_plain_tcp`) path on `modal_endpoint.py` (both `rest_plain_tcp` and `rest_https_edge` are in the kept 4-member `CohortKind`); it is renamed in T022, not deleted. Gate green. **DONE 2026-05-30.** `git rm`'d **85** milestone-named legacy test files (`test_m3_*`/`test_m4_*`/`test_m5*`/`test_m6_*`/`test_m6_1*`, glob `test_m[0-9]*` minus `test_m6_2`). `test_modal_endpoint_m5_2.py` (no digit after `test_m` → not matched) and the 17 `test_m6_2_*` retained. After deletion, `ls $TST | grep -cE '^test_m[0-9]'` = **17** (the `test_m6_2_*` set; reaches 0 at T022's rename — that's where the SC-001 test-half lands). **Gate**: `make lint` clean, `make typecheck` clean (`mypy --strict` 53 src files — test deletion doesn't touch src). **pytest collection remainder (→ T022/T022b):** 9 `test_m6_2_*` files + retained-name `test_metrics`/`test_mock_engine` (and `test_rtt_probe`/`test_chat_corpus_parity`) still import deleted `m3_types`/`m6_1_types`/`m6_1_2_types`/`m5_1_grpc_cohort` → repointed at the homes in T022; `test_reporter` (`write_wire_size_comparison_md` gone) gutted in T022b. Full `make test` green at the post-T024 checkpoint.
- [X] T022 [US2] Rename the 17 `test_m6_2_*.py` → generic names matching de-prefixed modules (`test_m6_2_sweep`→`test_sweep`, `test_m6_2_rpc_driver`→… , `test_m6_2_validate_cli`→`test_validate_cli`, `test_m6_2_cli`→`test_cli`, `test_monitor_m6_2_sweep_artifact_present`→`test_monitor_sweep_artifact_present`, …); also rename `test_modal_endpoint_m5_2.py`→`test_modal_endpoint_dual_transport.py` (milestone label dropped, coverage kept — confirm its dual-transport assertions still pass). Repoint all imports at the de-prefixed modules/homes. Gate green. **DONE 2026-05-30.** **Renamed 19 files** via `git mv`: the 17 `test_m6_2_X`→`test_X` (anchor_trajectory/artifact_schema/cli/crossover/iteration_order/kv_pressure/modal_integration/null_anchor/preemption_recovery/prompt_source/publish_cli/rest_driver_timing/resume/retry_policy/segment_aggregation/sub_probe/validate_cli), the mid-name `test_monitor_m6_2_sweep_artifact_present`→`test_monitor_sweep_artifact_present`, and `test_modal_endpoint_m5_2`→`test_modal_endpoint_dual_transport` (dual-transport assertions pass). → **`ls $TST | grep -cE '^test_m[0-9]'` = 0 (SC-001 test-half ✓).** **Repointed 13 import lines** in 10 files at the `types` home (with `as` aliases preserving the milestone *symbol* names for T028a): `m6_1_2_types.M6_1_2_COHORTS`→`types.COHORTS as M6_1_2_COHORTS`, `M6_1_2CohortKind`→`CohortKind as …`, `M6_1_2NetworkPath`→`types.M6_1_2NetworkPath` (already milestone-named in the home); `m6_1_types.M6_1Cell`/`M6_1_CELLS`→`types.Cell`/`CELLS as …`; `m3_types.RTTRecord`/`BenchmarkCell`→`types.…` (in `test_rtt_probe`/`test_mock_engine`). Removed the vestigial `from m5_1_grpc_cohort import _send_chat_rpc` guard line in `test_chat_corpus_parity` (the test builds the proto request inline; the deleted-module guard was never called). **Tests are now `m[0-9]`-import clean.** **Gate**: `make lint` clean (9 auto-fixed I001 import-order from the repoint), `make typecheck` clean (53 src files), `pytest` **544 passed / 1 failed** — the sole failure is `test_reporter::test_write_wire_size_comparison_md` (imports the deleted M1 writer), which is **T022b's** gut/repoint. Full green after T022b.
- [X] T022b [US2] **(T017-tail)** Gut/repoint `$TST/test_reporter.py`: it currently tests the legacy M1 writers (`write_json`/`write_csv`/`write_summary_md`/`write_wire_size_comparison_md`) deleted in T020 — drop those cases (their coverage is M1-era, gone with the writers) and either repoint the file at the consolidated `reporter.py`'s M6.2 surface or fold its remaining coverage into the renamed `test_m6_2_artifact_schema`→`test_artifact_schema` (T022). Must land with/after T020's reporter-writer deletion. Gate green. **DONE 2026-05-30 — NARROWED by ADR 0007.** The premise shifted: `write_json`/`write_csv`/`write_summary_md` are **NOT** deleted — they're live via the retained `bench` no-arg path (ADR 0006/0007), so their 3 tests (`test_write_json_valid`/`test_write_csv_columns`/`test_write_summary_md_contains_proxy_and_native`) are **kept** (they exercise the live M1 reporter surface). Only `write_wire_size_comparison_md` was deleted in T020 → removed its single test `test_write_wire_size_comparison_md` plus the now-orphaned `_make_summary` helper (used only by it). `test_reporter.py` 162→91 lines; its imports (`write_csv`/`write_json`/`write_summary_md` from `reporter`, `compute_summaries`/`BenchmarkRun`/`RequestResult`/`RunMeta` from `metrics`) all resolve, no unused. **Gate**: `make lint` clean, `make typecheck` clean (53 src files), `pytest tools/benchmark/tests` **544 passed / 0 failed** — **the benchmark suite is fully green** (first time since Phase 4 began).
- [X] T023 [P] [US2] Add new-home test coverage: `$TST/test_types.py`, `test_prompts.py`, `test_timing.py`, `test_exceptions.py` (data-model Entity 8) — at minimum assert `CohortKind` has 4 members, the unified builder is deterministic, and `SchemaValidationFailed` raises. Gate green. **DONE 2026-05-30.** Created the 4 new-home test files (**+23 tests**), one per Entity-1 facade home, exercising the *de-prefixed* surface directly (no `m[0-9]` imports — keeps the T024 strict-zero grep clean): **`test_types.py`** (6) — `CohortKind` has exactly 4 members via `typing.get_args` **and** `set(COHORTS) == set(get_args(CohortKind))` (SC-003 / clarify Q1); `cohorts_at_concurrency(1)` collapses the two gRPC cohorts to a single `default_grpc` (FR-011) while `c∈{2,4,8}` yields all 4 gRPC-first; `RPCResult` is frozen (constructs + rejects mutation via `FrozenInstanceError`). **`test_prompts.py`** (4) — `build_chat_prompt` deterministic (same seed → identical bytes), per-seed-varying (50 distinct seeds → 50 distinct prompts), seed echoed into the text; `DEFAULT_CHAT_MAX_TOKENS == 64` (hoist survived). **`test_timing.py`** (10) — `has_engine_stats` gate both ways; `PerSegmentDelta.from_checkpoint` ns→ms for all 5 segments + engine segments `None` without stats; REST + gRPC extractors round-trip a full checkpoint, miss (`None`) on absent sub-object / partial fields / missing keys; `timing_checkpoint_to_payload(None)` passthrough + `TimingCheckpoint(**payload)` re-hydration equality (the RPC-driver contract). **`test_exceptions.py`** (3) — `SchemaValidationFailed` subclasses `RuntimeError`, raises with message, and is trapped by a broad `RuntimeError` catch. **Gate**: `make lint` clean (1 auto-fixed I001 import-order in `test_exceptions.py`), `make typecheck` clean (`mypy --strict` 53 src files — tests untyped-checked), `uv run pytest tools/benchmark/tests` **567 passed / 0 failed** (= 544 prior + 23 new; **fully green**). **Note (CWD gotcha for T024):** `test_validate_cli::…crossover_has_six_records` + `test_publish_cli::…crossover_table_six_records` load the M6.1.3 baseline via the repo-relative `docs/benchmarks/m6_1_3-attribution-closure.json` — they pass only when pytest's CWD is the **repo root** (as `make test` runs it); running from `tools/benchmark/` yields empty `protocol_crossover` (0≠6). Pre-existing on clean HEAD, unrelated to T023; flagged here so the T024 gate is run from repo root.
- [X] T024 [US2] Verify SC-001/SC-002 **and the strict, carve-out-free I3** (the T019 facade-home carve-out is now dissolved — T020 hoisted the home defs in-place, so this asserts the literal zero): `ls $SRC/*.py | grep -cE '/m[0-9]'` → 0; `ls $TST/*.py | grep -cE '/test_m[0-9]'` → 0; **anchored import grep across src+test → 0 with NO exceptions** (the four facade homes `types`/`prompts`/`timing`/`exceptions` no longer re-export from legacy): `grep -rnE '^[[:space:]]*(from|import)[[:space:]]+[A-Za-z0-9_. ]*\bm[0-9]' $SRC $TST | grep -vE 'm6_2-token-budget|m6_1_3-attribution|docs/benchmarks' | wc -l` → 0; record final `ls $SRC/*.py | wc -l` (~25–28) and `ls $TST/*.py | wc -l` (~35–37) against T003; `make lint typecheck test` green. **DONE 2026-05-30 — Phase 4 / US2 closure checkpoint (verification-only, no source change).** **SC-001 (zero milestone-named files, both halves):** `ls $SRC/*.py | grep -cE '/m[0-9]'` = **0** ✓; `ls $TST/*.py | grep -cE '/test_m[0-9]'` = **0** ✓. **Strict carve-out-free I3:** the anchored `m[0-9]` import grep across `$SRC` + `$TST` (excluding the doc-path artifact false-positives `m6_2-token-budget`/`m6_1_3-attribution`/`docs/benchmarks`) = **0 matches, NO exceptions** — the T019 four-home carve-out (`types`/`prompts`/`timing`/`exceptions`) + the `reporter.py`/`symmetric_prompts.py` residuals are fully dissolved (T020 in-place hoist + T020a strip confirmed). The literal-zero form of contract I3 now holds. **SC-002 (final counts vs T003 baseline 84 src / 137 test):** `ls $SRC/*.py | wc -l` = **35** (−49, −58%), `ls $TST/*.py | wc -l` = **41** (−96, −70%). **Counts land above the task's `~25–28`/`~35–37` estimate by design** — the estimate predated ADR 0006/0007, which retained more surface than the original plan assumed: the `bench`/`compare`/`compare-cross`/`compare-three-way` + `sweep` tooling stayed (ADR 0006), `m3_sweep`'s live servicers were de-prefixed into a NEW retained `grpc_servicers.py` rather than deleted (ADR 0007), and T023 added 4 new-home test files. The SC-002 *directional* intent (substantial net reduction, zero contrived merges) is met: −58% src / −70% test with every surviving module cohesive. **Gate (full, run from repo ROOT so the relative-path M6.1.3 crossover baseline resolves — see T023 CWD note):** `make lint` clean (ruff check + `ruff format --check`, 141 files), `make typecheck` clean (`mypy --strict`, 53 source files), `make test` **685 passed / 0 failed** (9 pre-existing coroutine RuntimeWarnings, non-fatal) across all packages (proxy + frontend + client + integration + `tools/benchmark`). **Phase 4 / US2 is complete** — the codebase is milestone-agnostic (US1 spirit) AND legacy-free (US2 letter): zero `m[0-9]` module names, zero `m[0-9]` import statements, full suite green.

**Checkpoint**: Zero milestone-prefixed modules/tests; suite green; counts recorded.

---

## Phase 5: User Story 3 — Recoverability + documentation (Priority: P3)

**Goal**: The BC breaks are documented and the tag recovery path is verified intact.

**Independent Test**: `git show milestone/m5.2-transport-tuning:…m5_2_sweep.py` returns content; ANALYSIS.md has the refactor subsection; preserved artifacts resolve (SC-007, SC-008, SC-010).

- [X] T025 [US3] Document the refactor in two places (FR-015 + constitution ADR requirement): (a) add the "Bench-harness refactor (v0.0.1)" subsection to `ANALYSIS.md` — summary, BC breaks (renamed modules, unified prompt format, dropped `tuned_grpc_channels`/`tuned_grpc` cohort members, flat CLI), and the `milestone/m*` tag recovery path; (b) author `docs/decisions/0006-bench-harness-refactor.md` (ADR) recording the non-obvious architectural choices — the three-disposition model (de-prefix / hoist-then-delete / delete), the 4-member `CohortKind` collapse, the reporter consolidation, the forward-only / no-BC-shims policy, and the data-pointer-preservation rule — citing `research.md` for detail (constitution Development Workflow: ADR MUST for non-obvious choices). **DONE 2026-05-30 (docs-only, no source change).** **(a)** Added the **"Bench-harness refactor (v0.0.1)"** subsection to `ANALYSIS.md` under "Repo housekeeping": the 84→35 src / 137→41 test reduction with the function-organized forward layout; the four BC-break classes (renamed modules/import paths; unified seed+digest `build_chat_prompt` changing REST-cohort prompt bytes; 6→4-member `CohortKind` dropping `tuned_grpc_channels`/`tuned_grpc`; flat `bench`-default + `{compare,compare-cross,compare-three-way,sweep}` CLI retiring the `--mN` flags); the `milestone/m*` recovery path with a worked `git show milestone/m5.2-transport-tuning:…/m5_2_sweep.py` example; and the data-pointer-preservation note. Links to ADRs 0006/0007/0008. **(b)** Authored the ADR — **renumbered `0006`→`0008`** because `0006`/`0007` were already taken by the two mid-flight correction ADRs written during T018/T020 (`0006-cli-keep-bench-add-sweep.md`, `0007-m3-sweep-servicer-relocation.md`); the task's hardcoded `0006` filename predated that drift. `docs/decisions/0008-bench-harness-refactor.md` records all six non-obvious choices (three-disposition model, homes-created-first ordering, 4-member `CohortKind` collapse, one-reporter/one-builder/one-error consolidation, forward-only/no-BC-shims policy, data-pointer-preservation rule), the headline reduction + the as-built deviation from research.md R2/R4 (M1 `bench` writers KEPT live, not deleted — per ADR 0007), the symbol-name-de-prefix deferral (T028a), and a References block citing `research.md` R1–R8 + the spec FRs/SCs. Includes an explicit numbering note. **Verification**: all 3 ADR links + the 3 data pointers (`m6_2-token-budget.{json,md}`, `m6_1_3-attribution-closure.json`) resolve on disk; the documented recovery command returns the intact M5.2 sweep harness. No gate clause on this task; docs-only so no test/graphify-code impact.
- [X] T026 [US3] Verify SC-010 preserved data pointers: `ls docs/benchmarks/m6_2-token-budget.json docs/benchmarks/m6_2-token-budget.md docs/benchmarks/m6_1_3-attribution-closure.json` all exist; grep `ANALYSIS.md` benchmark-artifact links all resolve to existing files. **+ restore the report writeups erroneously deleted in v0.0.0 (FR-020).** **DONE 2026-05-30.** **(a)** The 3 explicit data pointers exist ✓. **(b) Discovery:** the literal link check failed — **17 distinct `docs/benchmarks/` hyperlink targets in `ANALYSIS.md` (15 `.md` report writeups + 2 `.json` data files) did not resolve.** Root-caused as **pre-existing v0.0.0 debt, not v0.0.1**: `git ls-tree main -- docs/benchmarks` shows only the `m6_2-*.md` files tracked, and `git diff main...HEAD` added zero benchmark links — the `v0.0.0` cleanup deleted the per-milestone report writeups (+ `m3-channel-tuning.json` / `m6_1_2-methodology-discipline.json`) but left `ANALYSIS.md`'s hyperlinks to them in place (owner oversight, acknowledged). **Resolution (user decision): recover the files, not rewrite the links.** Restored **16 files** verbatim via `git checkout milestone/m6.2-token-budget -- <path>` (the last tag carrying the complete set before the deletion; identical content to each file's own `milestone/m*` recovery path, so zero narrative drift): the 14 `.md` report writeups (`m3-channel-tuning{,-time}`, `m4-time-axis-tuning`, `m5-cross-host-validation`, `m5_1-rest-vs-grpc`, `m5_2-transport-vs-tuning`, `m6-real-engine-mini-validation`, `m6_0a-dispatch-correction`, `m6_1-real-prompt-embeds`, `m6_1_1-audit-2026-05-16-seq-dispatch`, `m6_1_1-engine-cost-instrumentation`, `m6_1_2-methodology-discipline`, `m6_1_3-attribution-closure{,-validate}`) + the 2 `.json` data files. **`summary.md` deliberately excluded** — it appears in `ANALYSIS.md` only as prose/code-span (lines 5, 64), never as a hyperlink, and is documented as folded into §M1/§M3 + tag-recoverable; restoring it would contradict that decision. **Verification after recovery:** every genuine markdown hyperlink `](docs/benchmarks/…)` resolves — **0 broken** (was 17); both `#anchor` links resolve to real headings in the recovered files (`m6_1-real-prompt-embeds.md` → "## Methodology Supersedence (M6.0a — Dispatch-Correction)"; `m6_0a-dispatch-correction.md` → "## 5. Implication for M6.x findings — dispatch-sensitive vs dispatch-robust"). **Spec updated (per user):** added **FR-020** (the report writeups + `.json` siblings MUST exist so `ANALYSIS.md` links resolve; records the v0.0.0-oversight + v0.0.1-restoration + the `summary.md` exclusion), extended FR-019 with class (d), and strengthened SC-010(c) to "every hyperlink resolves (incl. `#anchor`s)". `docs/benchmarks/` is in the FR-018/T030 edit fence, so the restoration is in-scope. All 16 files staged.
- [X] T027 [P] [US3] Verify SC-007 recoverability: `git show milestone/m5.2-transport-tuning:tools/benchmark/src/vllm_grpc_bench/m5_2_sweep.py | head` returns the legacy harness; confirm the M5.2 6-member cohort enum + M5.2 prompt builder are present at that tag. **DONE 2026-05-30 — PASS (verification-only).** `git show milestone/m5.2-transport-tuning:…/m5_2_sweep.py` returns the intact legacy harness (1526 lines, docstring "M5.2 sweep orchestrator — five-cohort transport-vs-tuning sweep"). **6-member cohort enum confirmed** at `m3_types.py:748` (note: defined in `m3_types`, not `m5_2_types`): `M5_2CohortKind = Literal["rest_https_edge","rest_plain_tcp","default_grpc","tuned_grpc_multiplexed","tuned_grpc_channels","tuned_grpc"]` — the 4 forward members + the 2 dropped-in-v0.0.1 (`tuned_grpc_channels`/`tuned_grpc`). **M5.2 prompt builder confirmed** at `m3_sweep.py:324`: `def build_chat_prompt(*, iteration: int, cell_id: str)` — the M5.2 `iteration`/`cell_id` form that v0.0.1 replaced with the unified seed+digest builder. SC-007 holds: everything the refactor broke on `main` is recoverable at the M5.2 tag.

**Checkpoint**: Recovery path proven; BC breaks documented.

---

## Phase 6: Polish & Cross-Cutting

**Purpose**: Final audits, gate, and ship.

- [X] T028 [P] Resolve the research.md R2 audit items: decide keep-and-de-prefix vs delete for `m6_1_torch_pin` (does `validate`/`sweep` need the torch gate?), `compare.py`, `ci.py` (does a surviving CLI path use them?). Apply the decision; gate green. **PARTIAL (T020, ADR 0007):** `m6_1_torch_pin` was confirmed orphaned (sole importer `m6_1_sweep`, now deleted) → deleted with the legacy batch. `compare.py` resolved to **keep** (ADR 0006 — `compare*` subcommands). Remaining for T028: confirm `ci.py` (used by `engine_cost`/`metrics`/`ttft` live paths → likely keep) and finalize. **DONE 2026-05-30.** **`ci.py` → KEEP** (confirmed live): the 95% CI t-estimator is imported by `engine_cost.py:25` (`from vllm_grpc_bench.ci import estimate`), which is itself live — imported by `types.py:24` (`EngineCostSpan`) and `rpc_driver.py:50` — plus its own `test_ci.py`. Milestone-clean name, reachable from the sweep/validate/bench paths via `engine_cost`. **`compare.py` → KEEP** (re-confirmed): `__main__.py:10` imports `compare`/`compare_cross`/`compare_three_way` for the retained `compare`/`compare-cross`/`compare-three-way` subcommands (ADR 0006). **NEW FINDING — `ttft.py` → DELETE** (audit caught what R2 missed because it's non-milestone-named): the module had **zero importers anywhere** (src/tests/scripts/`__init__`/pyproject — `grep vllm_grpc_bench.ttft` = 0 outside itself) and no `test_ttft.py`; its own docstring states it serves "the M3 reanalyze and M4 sweep paths" (both deleted in T020) and its `ttft_samples`/`ttft_estimate` consume the M3-era `RunCohort`. Pure orphaned dead code. **User decision: delete** (recoverable via `milestone/m*` tags). `git rm tools/benchmark/src/vllm_grpc_bench/ttft.py` → **src count 35 → 34**; removes `ttft`→`metrics` from T029's merge candidates (merging dead code would have been a contrived merge). `ci.py` stays live after the delete (still imported by `engine_cost` + `test_ci`; only its `ttft.py` importer went away). **Gate**: `make lint` clean, `make typecheck` clean (`mypy --strict` **52** src files, was 53), `make test` **685 passed / 0 failed** (run from repo root; `ttft` had no test so the count is unchanged).
- [ ] T028a [P] **Symbol-name de-prefix pass (deferred from Phase 3 — surfaced during T011/T015pre).** The Phase 3 de-prefix tasks (T009–T018) de-prefix **module names + imports only**; milestone-flavored **symbol** names were intentionally left intact (`M6_2SweepArtifact`, `M6_2MeasurementPoint`, `M6_2SweepMode`, `M6_2_MAX_TOKENS_AXIS`, `M6_2RunMeta`, `M6_1_2CohortKind`/`M6_1_2_COHORTS`/`M6_1Path` aliases, `M5_2*`, …). The T019/T024 grep gate is import-only (case-sensitive lowercase `\bm[0-9]`), so uppercase `M6_2`/`M5_2` symbols pass — but a *truly* milestone-agnostic codebase (US1 spirit, FR-004) wants `SweepArtifact`, `MeasurementPoint`, `SweepMode`, `MAX_TOKENS_AXIS`, etc. **Scope**: rename ~30 public symbols (8 dataclasses + ~13 constants + 5 literals in `sweep_types.py`, plus the aliased cohort/path names threaded through `sweep.py`/`sweep_types.py`) across ~20 importing src+test files; drop the now-redundant home aliases (`COHORTS as M6_1_2_COHORTS` → just `COHORTS`; `Path as M6_1Path` → resolve the `pathlib.Path` collision by importing the domain enum under a clean name, e.g. `CellPath`). **Approach**: one symbol at a time via `replace_all`, gate green after each cluster; verify no name collisions first (e.g. `Path`, `Cell`). **Decision gate**: confirm with the user whether to do this at all (large mechanical churn, BC-break on every symbol — recoverable via tags) vs. accept the import-only de-prefix as "milestone-agnostic enough" for v0.0.1 and leave symbol names to a later milestone. If declined, record the decision in the ADR (T025) and update SC-003's interpretation. Gate green. **DECISION GATE RESOLVED 2026-05-30 — APPROVED (owner): PROCEED with the symbol cleanup.** Recorded in ADR 0008 (the "Symbol-name de-prefix" consequence bullet flipped from *deferred* → *approved*) and SC-003's interpretation in spec.md (milestone-agnosticism now extends to public symbol names — no `M6_2*`/`M6_1*`/`M5_2*` public symbol survives). **Execution still pending** (this task stays `[ ]` until the rename lands): perform the ~30-symbol rename one cluster at a time via `replace_all`, gate green after each, collisions (`Path`→`CellPath`, `Cell`) resolved first.
- [ ] T029 [P] Evaluate the SC-002 thin-module merge candidates (`symmetric_prompts`→`prompts`, ~~`ttft`→`metrics`~~) and merge **only** where cohesion improves (no contrived merges — SC-002 directional). (`engine_cost` is its own home per T013 — not a merge candidate.) Gate green. **Note (T028):** `ttft`→`metrics` is **no longer a candidate** — the T028 audit found `ttft.py` was orphaned dead code (zero importers, M3/M4-only) and **deleted** it, so only `symmetric_prompts`→`prompts` remains to evaluate.
- [ ] T030 Final full gate (FR-012): `cd tools/benchmark && make lint typecheck test` green with no new suppressions vs the T001 baseline; `python -m vllm_grpc_bench --validate --skip-deploy` completes; verify FR-018 scope — `git diff --name-only main...HEAD | grep -vE '^(tools/benchmark/|docs/benchmarks/|ANALYSIS\.md|docs/decisions/|specs/029-|Makefile$|scripts/python/modal_bench_grpc_server\.py$|scripts/python/modal_bench_rest_grpc_server\.py$|\.specify/|CLAUDE\.md$|specs/028-)'` returns empty (no `.proto`/`vllm`/`packages/*`/other-`specs` edits). **The `Makefile` is in-fence (ADR 0006 / T018): the CLI simplification added `sweep`/`sweep-validate` targets and kept `bench`/`bench-ci`/`bench-compare`. The two `scripts/python/modal_bench_*_server.py` deploy scripts are in-fence (ADR 0007 / T020): the `m3_sweep` servicer relocation to `grpc_servicers.py` forced repointing their `ChatServicer`/`CompletionsServicer` import.** **Three Spec-Kit-bookkeeping paths are also in-fence — they are machine-managed side effects of scaffolding feature 029, not refactor edits: `.specify/feature.json` (the active-feature pointer, bumped `028`→`029` by `/speckit-specify`), `CLAUDE.md` (the `<!-- SPECKIT START/END -->` block's "current plan" path, bumped `028`→`029` by `/speckit-plan`), and `specs/028-post-m6.2-cleanup-v0.0.0/tasks.md` (the prior beat's T040–T044 post-merge checklist flipped to `[X]`). Reverting the first two would mis-point Spec Kit at the retired feature, so they are admitted rather than reverted (same precedent as the Makefile/Modal-script carve-outs above).**; run `graphify update .` from repo root (constitution Development Workflow).
- [ ] T031 Push branch and open PR (FR-017): `git push -u origin chore/post-m6.2-cleanup-v0.0.1`; `gh pr create --base main --title "v0.0.1 — Bench-harness refactor"` with a body listing the BC-break ledger (module renames, unified prompt, dropped cohort members, flat CLI) and the tag recovery path.
- [ ] T032 **[POST-MERGE]** After merge: `git switch main && git pull`; create + push the annotated tag `git tag -a v0.0.1 -m "v0.0.1 — bench-harness refactor"` && `git push origin v0.0.1` (FR-014, SC-008).

---

## Dependencies & Execution Order

- **Phase 1 (Setup)** → **Phase 2 (Foundational homes)** → **Phase 3 (US1)** → **Phase 4 (US2)** → **Phase 5 (US3)** → **Phase 6 (Polish)**.
- **Hard ordering**: homes (T004–T008) before any repoint/de-prefix (T009–T018); all live code de-prefixed/repointed (US1) before any legacy deletion (T020) — deleting earlier breaks live imports.
- **US3 + most of Polish** can begin once US2's T024 is green.

## Parallel Opportunities

- **T004, T005** parallel (independent new files); **T006, T007** serialize lightly (T007 may reference `types`).
- **T010** (shared-infra repoint, distinct files) and **T016** (independent `m6_2_*` leaf modules) are `[P]`.
- **T023** (new-home tests) parallel with T022 rename once modules exist.
- **T026, T027** (verification-only) and **T028, T029** (independent audits) are `[P]`.

## Implementation Strategy

- **MVP = Phase 1 + 2 + 3 (US1)**: a milestone-agnostic, green live harness — delivers the core value (de-tangled forward codebase) even before deletion.
- **Incremental**: US2 (deletion + counts) is the headline reduction; US3 (docs/recovery) and Polish (audits, ship) follow.
- Keep every commit bisectable and green (FR-008); one logical commit per task or small `[P]` batch, matching quickstart.md Steps 1–9.

## Notes

- Task count: **33** (vs the PLAN's ~17 estimate — the gap is the folded-in de-prefix + reporter/CLI consolidation, per clarify Q3/Q4/Q5). T010a was re-scoped into Phase 4 as T020a after consumer analysis (see T010a note).
- Per US1/US2/US3: US1 = 10 active tasks (T009–T019; T010a re-scoped → T020a), US2 = 6 (T020, T020a, T021–T024), US3 = 3 (T025–T027); Setup 3, Foundational 5, Polish 5.
- The `docs/benchmarks/*` artifact path strings inside `validate.py` and the sweep baseline defaults are **not** milestone-prefixed *imports* and must survive the T019/T020 grep checks (FR-019) — exclude them when asserting "zero `m[0-9]` references."

### Facade-home carve-out (T019)

**Why it exists.** The four Entity-1 generic homes are *facades* during US1: they re-export live symbols from the legacy modules that still define them, and the real definitions only move in (the "hoist") when those legacy modules are deleted in **Phase 4 (T020)** — see data-model Entity 1 ("the definitions move here when those modules are deleted (Phase 4)") and Entity 7 ("…after hoist"). So at the **T019** US1 checkpoint (which runs *before* any legacy deletion), these homes necessarily still contain `from …mN… import …` lines. A literal "zero `m[0-9]` imports across all live modules" is therefore **unachievable at T019 by design** — the carve-out makes the gate honest rather than forcing a premature hoist.

**The carve-out set (exactly four modules), with their intentional legacy re-exports as of 2026-05-30:**

| Home | Re-exports (legacy source) | Hoist lands |
|---|---|---|
| `types.py` | `m3_types` (`EndpointTuple`, `NetworkPath`, `Path_`, `RESTCohortRecord`, `RestHttpsEdgeCohortRecord`, `RTTRecord`, `RunCohort`), `m6_1_2_types` (cohort/cloud/omissions + the 3 network-probe dataclasses, T014), `m6_1_types` (`CELLS`, `Cell`, `Path`), `m6_sweep` (`RPCResult`) | T020 |
| `prompts.py` | `m3_sweep.DEFAULT_CHAT_MAX_TOKENS`, `m6_rpc_driver._build_chat_prompt` (alias `build_chat_prompt`) | T020 |
| `timing.py` | `m6_1_1_timing.*`, `m6_1_1_types` (`PerSegmentDelta`, `TimingCheckpoint`) | T020 |
| `exceptions.py` | `m5_2_regen.M5_2SchemaValidationFailed` (alias `SchemaValidationFailed`) | T020 |

  (`engine_cost.py`, `seq_len.py`, `network_probe.py`, `rpc_driver.py` are **not** in the carve-out — they were made `m[0-9]`-import-clean leaves/consumers in T013/T014/T014a/T012.)

  **Plus a fifth transitional residual: `reporter.py`** — but for a *different* reason than the facade homes. The additive T017 (user decision) spliced the M6.2 writers into `reporter.py` and de-prefixed all its M6.2 imports, but **left the legacy M1–M5.2 writers in place** (under a `# === LEGACY … ===` marker) because they're still consumed by `__main__` (T018), the retained `test_reporter.py` (T022), legacy `m5_2_regen` (T020), and legacy tests (T021). Those legacy writers keep `reporter.py`'s `from …m3_types import …` (line ~16) + a function-local `from …m5_2_regen import …`. This residual is **deleted (not hoisted)** when its consumers go in T020/T021/T022 — so `reporter.py` is `m[0-9]`-clean by T024, not by T019. Treat it as a fifth allowed residual at the T019 grep.

**Use the anchored grep.** The loose T019/contract grep `(import|from).*\bm[0-9]` also matches *prose* — e.g. `channel_config.py:222` ("Hoisted from ``m3_sweep``…", a real T008 move, not an import) and `rest_cohort.py:70` ("…touching m6_1_1_timing at module-load time", a comment; the actual late import resolves to `timing`). Anchoring on real statements — `^[[:space:]]*(from|import)[[:space:]]+[A-Za-z0-9_. ]*\bm[0-9]` — drops those false positives, leaving only genuine imports to classify against the carve-out set.

**The carve-out dissolves at Phase 4 — and that's the real gate.** T020 must **hoist the four homes' definitions in-place** (move `RPCResult` into `types.py`, the `m6_1_1_timing` surface into `timing.py`, etc.) in the same change that `git rm`s the legacy sources, so the homes survive deletion (T020's own `mypy --strict` "zero dangling imports" check forces this). After that, **T024 enforces the strict, carve-out-free form of contract I3**: zero `m[0-9]` import statements across *every* src+test module, no exceptions. T019 = "functional modules clean, facade homes are the only allowed residual"; T024 = "literally zero."
