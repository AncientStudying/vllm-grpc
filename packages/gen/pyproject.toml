[project]
name = "vllm-grpc-gen"
version = "0.1.0"
requires-python = ">=3.12"
description = "Generated protobuf stubs for vllm-grpc"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/vllm_grpc"]
